2026-09-17T00:00 ui_kivy/pantallas_riesgo.py (archivo nuevo, roadmap de cierre mobile ítem 3 — "Riesgo"): funciones puras de carga de caché y cálculo de color, sin estado propio (mismo criterio que pantallas_senal.py). cargar_cache_riesgo_equipo() lee riesgo_equipo_cache (degrada a {} si la tabla no existe). cargar_cache_riesgo_senal(db_path) usa core.signal_risk.SignalRiskAnalyzer.resumen_por_cable(). color_y_borde_por_riesgo(id_equipo, cache, rc,gc,bc, ancho_base) devuelve el color de cabecera + ancho de borde engrosado según impacto — equivalente a RiesgoDiagramaMixin._riesgo_color_y_borde (GTK), colores ya en escala 0-1 (Kivy), sin conversión hex. conn_colors_por_riesgo_senal(cache) mapea ejes (ELECTRICO/BALANCE/CANAL > ATENUACION > ANCHO_BANDA) a color de cable — equivalente a RiesgoSenalDiagramaMixin._riesgo_senal_conn_colors. simular_falla_equipo(db_path, id_equipo) corre GraphImpactAnalyzer.simular_falla_equipo y core.senal_estado.senales_caidas_por_equipos, devolviendo los datos crudos (separación función pura/UI). PopupResultadoSimulacion(Popup) reemplaza al Gtk.Dialog modal de _riesgo_on_simular_falla, con ScrollView + Label (mismo patrón que PopupDetalleSesion de pantallas_diagnostico.py).
2026-09-17T00:00 ui_kivy/pantallas_diagrama.py: integración de pantallas_riesgo.py — import, estado (_riesgo_color_activo/_riesgo_cache, _riesgo_senal_color_activo/_riesgo_senal_cache, _riesgo_senales_cache_dict) en DiagramaConexiones.__init__, 2 ToggleButton ("🎨 Riesgo equipo"/"🎨 Riesgo señal") + 1 Button ("🔺 Simular falla") en la toolbar junto a los de Señal, hooks en _CanvasDiagrama._draw_node (color de cabecera + ancho de borde) y _calc_conn_colors (color de cable, con prioridad sobre el color de selección — mismo criterio que GTK). _riesgo_on_simular_falla lee self._canvas._sel_id, muestra PopupResultadoSimulacion y puebla self._riesgo_senales_cache_dict mientras el popup está abierto (se limpia en on_dismiss) — _senal_conectores_caidos ya esperaba ese atributo vía getattr con default {}, así que la integración con el tachado de señal salió sin tocar ese método.
2026-09-17T00:00 ui_kivy/pantallas_planos.py (archivo nuevo, roadmap de cierre mobile ítem 4a — "Planos: PlanosListado"): PlanosListado/DialogoPlano, catálogo simple (nombre + imagen + orden), mismo patrón que ImagenesListado/DialogoImagen (Popup + grid_formulario, sin tabs — 3 campos sin acciones propias, mismo criterio de "diálogo simple" del alcance acordado en Fase 4). Selección de imagen vía FileChooserPopup + copia a IMG_DIR. Modelo.devolver_todos_los_planos/devolver_plano/alta_plano_retorna_id/modificacion_plano/eliminar_plano ya existían en core/modelo.py, sin cambios ahí. Wireado en ui_kivy/main.py: import, abrir_planos(), entrada "Planos" en el grupo "Infraestructura" del menú (mismo lugar que ui_gtk/cabledoc.py._abrir_planos, junto a Salas/Rack por Sala).
  Bug encontrado por el smoke test de esta misma entrega, corregido del lado mobile: imagen.path_archivo es NOT NULL en schema_db.sql, pero permitir guardar un plano sin imagen (como hacía el primer borrador de DialogoPlano._guardar, calcado del aviso de GTK "se va a guardar igual, pero va a aparecer en negro") revienta con IntegrityError en vez de guardar. DialogoPlano._guardar ahora bloquea antes de llamar a Modelo, con un mensaje que explica el motivo real. El mismo bug sigue presente en ui_gtk/planos_ui.py._DialogoPlano.run_and_destroy, sin tocar — fuera de alcance de esta entrega, queda para que Papi decida el comportamiento correcto (bloquear igual que mobile, o sacar el NOT NULL de imagen.path_archivo si un plano sin imagen es un estado válido).
2026-09-17T00:00 Validación de esta entrega (ítems 3 y 4a del roadmap de cierre mobile): (1) ast.parse + py_compile sobre los 4 archivos tocados/creados: OK; (2) pyflakes comparado contra git stash: cero advertencias nuevas (pantallas_diagrama.py incluso pierde una preexistente — "local variable 'n' is assigned to but never used" en _calc_conn_colors — al reescribir ese método); pantallas_riesgo.py/pantallas_planos.py sin ninguna advertencia; (3) sandbox con acceso de red: pip install kivy (2.3.1) + apt xvfb; (4) smoke test funcional bajo Xvfb contra una base SQLite generada desde data/schema_db.sql: PlanosListado abre/cierra; DialogoPlano bloquea sin nombre y sin imagen (sin excepción, sin fila huérfana en `imagen`); alta real con imagen copiada a IMG_DIR persiste y se relee correctamente; cargar_cache_riesgo_equipo/cargar_cache_riesgo_senal degradan a {} sin datos; color_y_borde_por_riesgo/conn_colors_por_riesgo_senal correctos en aislamiento; DiagramaConexiones (vista global, grafo vacío) abre con los 3 controles nuevos sin excepción; "Simular falla" sin selección avisa y vuelve sin excepción. (5) Segundo smoke test con datos reales (2 equipos + 1 conector cada uno + 1 cable + 1 fila de riesgo_equipo_cache nivel "Crítico"): DiagramaConexiones(id_equipo=...) carga 2 nodos/1 conexión; con el toggle "🎨 Riesgo equipo" activo, color_y_borde_por_riesgo resuelve el nodo en riesgo a rojo real (0.85, 0.15, 0.15) con el borde engrosado, ejercitando el branch nuevo de _draw_node contra datos reales (no sólo la función aislada); toggle de riesgo de señal y "Simular falla" sobre el equipo con dependiente real, ambos sin excepción. git apply --check sobre un clone fresco de main: OK.
2026-09-08T22:15 pantallas_comunes.py/equipos_ui.py/catalogo_equipos_ui.py/editor_masivo_conectores_ui.py (plan_paneles_vectoriales_v3.md — piezas de UI de Fase 0/1 que habían quedado sin construir): Papi reportó que con la imagen SVG del equipo y las fichas SVG cargadas, los símbolos de conector salían todos al mismo tamaño en vez de a escala real. Investigación: Modelo.resolver_mm_por_pixel()/Modelo.calcular_radio_simbolo_px() (Fase 0/1, ya mergeadas) estaban correctamente cableadas en imagen_conectores_ui.py y editor_masivo_conectores_ui.py, pero no existía ningún control de UI para cargar ni ancho_mm/alto_mm/profundidad_mm del equipo ni imagen.mm_por_pixel — sin datos, resolver_mm_por_pixel() siempre devolvía None y calcular_radio_simbolo_px() caía siempre al radio_default_px fijo, dibujando todos los símbolos al mismo tamaño genérico sin importar tamano_relativo. Esta entrega cierra el hueco:
  - pantallas_comunes.py: helpers compartidos _fmt_mm(v)/_parse_mm(texto) (formato de mm para Gtk.Entry — sin ceros de más, acepta coma decimal al leer — usados por los 2 diálogos de abajo).
  - equipos_ui.py (_DialogoEquipo): sección nueva "📏 Dimensiones físicas" en la pestaña Datos (campos Ancho/Alto/Profundidad en mm + botón "Usar ancho estándar 19″" que precarga Modelo.ANCHO_RACK_19_MM), entre "Equipo usado" y "Riesgo de falla" — filas del grid renumeradas de 11-19 a 15-23 para hacerle lugar. Carga con Modelo.obtener_dimensiones("equipo", id_equipo) al editar; guarda con Modelo.actualizar_dimensiones("equipo", id, ...) tanto en alta como en edición, sin tocar la firma de alta_equipo_retorna_id/modificacion_equipo (mismo criterio ya usado para es_modulo_de_frame/chk_equipo_critico).
  - catalogo_equipos_ui.py (_DialogoCatalogoEquipo): mismos 3 campos + atajo 19″ en el editor de molde, debajo de la miniatura de foto. Carga/guarda vía Modelo.obtener_dimensiones/actualizar_dimensiones("equipo_catalogo", ...) — el molde es la fuente que se hereda por COALESCE en cada equipo instanciado que no tenga su propio valor (§2.2 del plan).
  - editor_masivo_conectores_ui.py: nueva _DialogoMedicion(Gtk.Dialog) (pide "¿cuánto mide en mm?" mostrando la distancia marcada en px nativos) + herramienta "📏 Medir en la imagen…" (§3.4 del plan) como Gtk.ToggleButton en EditorMasivoConectoresBase, junto a "▦ Colocar fila / grilla…". En modo medición, los clics en la imagen no colocan conectores: el primer clic marca el primer punto (dibujado como marca roja en _dibujar_overlay), el segundo calcula distancia_px = √((x2-x1)²+(y2-y1)²) en píxeles NATIVOS de la imagen (vía self._viz.w2i, no de pantalla — así no depende del zoom con el que se midió) y abre _DialogoMedicion; al aceptar, Modelo.actualizar_mm_por_pixel_imagen(id_img, mm_reales/distancia_px) persiste la calibración (fuente 1 de la cascada §3.3, gana siempre sobre ancho_mm) y se llama a _preparar_simbolos_conector() de nuevo para redibujar los símbolos ya a escala sin cerrar el editor. Clic derecho cancela la medición en curso. Nuevo helper compartido _id_imagen_actual() (id_imagen de la imagen cargada en el visor) reemplaza el lookup que antes estaba inline sólo en _on_clic_imagen — ahora también lo usa _on_clic_medir.
  - Validación: (1) py_compile sobre los 4 archivos: OK; (2) pyflakes comparado contra un clone fresco de HEAD: cero hallazgos nuevos (las 7 advertencias preexistentes de equipos_ui.py — imports de re/subprocess/MANUALES_DIR redefinidos localmente — idénticas, sólo desplazadas de línea); (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0/python3-gi-cairo/gir1.2-rsvg-2.0/xvfb y pip graphqlite; (4) smoke test funcional bajo Xvfb contra una base SQLite generada desde schema_db.sql + Modelo.asegurar_tablas_plano(): Modelo.actualizar_dimensiones/obtener_dimensiones para "equipo" y "equipo_catalogo" con round-trip completo; _fmt_mm/_parse_mm con valores enteros, decimales y coma decimal; (5) **smoke test real-integration bajo Xvfb con el equipo y el SVG reales que adjuntó Papi** (AUDIO MONITOR CTRL AIRE + monitor_blackmagic_AUDMON1RU.svg, una ficha XLR3 con símbolo cargado y un conector ETHERNET sin ficha): EditorMasivoConectoresImagen instanciada real, confirmado _simbolos_activos=True y radio del símbolo fijo en self.R (10px) sin calibración; simulados los 2 clics de "Medir en la imagen" (0,0)→(400,0) con 400mm reales cargados en el diálogo interceptado, confirmado mm_por_pixel=1.0 calculado y persistido en imagen.mm_por_pixel, y calcular_radio_simbolo_px() devolviendo 12.0px (24mm base / 2 / 1.0 mm/px) en vez del genérico — la escala real ahora responde a la calibración en vez de quedar siempre en el tamaño fijo; (6) _DialogoEquipo/_DialogoCatalogoEquipo instanciados reales bajo Xvfb: campo vacío antes de cargar dato, botón "Usar ancho estándar 19″" precarga "482.6", guardado real vía run_and_destroy() (con Gtk.ResponseType.OK simulado, incluyendo "44,5" con coma decimal) persistido y releído correctamente al reabrir el diálogo. APP_VERSION → 1.20260908221500.
 (EditorMasivoConectoresBase._dibujar_overlay): fix de bug encontrado por Papi en el smoke test visual pendiente de Fase 1 de plan_paneles_vectoriales_v3.md — el radio del símbolo vectorial se dibujaba fijo en escala 100% sin importar el zoom del visor, mientras que en imagen_conectores_ui.py (misma Fase 1) sí se multiplicaba por self._viz.zoom. Modelo.calcular_radio_simbolo_px() devuelve el radio en píxeles NATIVOS de la imagen por contrato explícito de su docstring ("el llamador multiplica por su factor de zoom al dibujar"); editor_masivo_conectores_ui.py se había olvidado de esa multiplicación al portar el mecanismo a la clase base compartida. Fix de una línea: radio_px = radio_img_px * self._viz.zoom, ahora consistente con imagen_conectores_ui.py. No afecta al círculo genérico de fallback (self.R, deliberadamente fijo en pantalla, sin cambios) ni requiere tocar EditorMasivoConectoresImagen/EditorMasivoConectoresCatalogo (heredan el fix de la clase base sin cambios propios). APP_VERSION → 1.20260908171500.
2026-09-07T19:30 modelo.py (plan_paneles_vectoriales_v3.md — Fase 0 y cálculo de escala de Fase 1): nueva tabla catalogo_simbolo_conector (id, id_tipo_conector UNIQUE, svg_fragmento, viewbox default '0 0 24 24', tamano_relativo default 1.0, color_sugerido, fecha_ultima_edicion) vía asegurar_tablas_catalogo_simbolos() idempotente, con CRUD completo (devolver_simbolos_conector/devolver_simbolo_conector/devolver_simbolo_de_tipo_conector/alta_simbolo_conector/modificacion_simbolo_conector/eliminar_simbolo_conector) y obtener_simbolos_conector(ids) para lookup masivo en una sola query (pensado para llamarse una vez por carga de pantalla, no por redibujo). asegurar_columnas_dimensiones() agrega ancho_mm/alto_mm/profundidad_mm (REAL nullable) a equipo/equipo_catalogo/frame/frame_catalogo y mm_por_pixel a imagen, vía ALTER TABLE ADD COLUMN idempotente (PRAGMA table_info primero). Getters/setters genéricos obtener_dimensiones(tabla,id)/actualizar_dimensiones(tabla,id,...) con whitelist _TABLAS_DIMENSIONES (equipo/equipo_catalogo/frame/frame_catalogo) — cualquier otro valor de `tabla` no ejecuta SQL, probado explícitamente pasando 'DROP TABLE equipo;--' y confirmando que la tabla equipo sigue intacta. obtener_mm_por_pixel_imagen/actualizar_mm_por_pixel_imagen (columna imagen.mm_por_pixel — fuente 1 de calibración, medición manual). resolver_mm_por_pixel(tabla,id_registro,id_imagen,ancho_imagen_px): cascada de 3 fuentes del plan §3.3 (imagen.mm_por_pixel medido a mano > ancho_mm de la entidad/ancho_imagen_px > None). calcular_radio_simbolo_px(tamano_relativo,mm_por_pixel,radio_default_px,tamano_base_mm): fórmula del plan §3.2 (diametro_mm = tamano_relativo × tamano_base_mm; radio_px = diametro_mm/2/mm_por_pixel), devuelve radio_default_px sin más si no hay calibración — nunca bloquea el render. Explícitamente NO se tocó la firma de alta_equipo/modificacion_equipo/alta_catalogo/modificacion_catalogo/instanciar_desde_catalogo (muchos call sites, alto riesgo sin poder hacer smoke test visual completo en este entorno) — las dimensiones se cargan/editan aparte con los getters/setters nuevos y NO se copian todavía automáticamente al instanciar un equipo desde un molde de catálogo; queda pendiente como follow-up (ver PROGRESS.md).
2026-09-07T19:30 pantallas_comunes.py (Fase 1 de plan_paneles_vectoriales_v3.md): nuevos helpers compartidos _crear_handle_simbolo(svg_fragmento, viewbox, color) (envuelve el fragmento en un <svg> válido, valida con xml.etree antes de pasarlo a Rsvg.Handle.new_from_data, devuelve None ante cualquier excepción) y _dibujar_simbolo_conector(cr, handle, wx, wy, radio_px) (Rsvg.Handle.render_document sobre un Rsvg.Rectangle centrado, misma API "no deprecada" que ya usa _ImagenSVG; devuelve False sin excepción si handle es None o falla el render). Reutilizan el _RSVG_DISPONIBLE/fallback ya existente en el módulo — sin Rsvg instalado, ambas funciones devuelven None/False igual que si el fragmento fuera inválido, nunca levantan.
2026-09-07T19:30 catalogo_simbolo_conector_ui.py (archivo nuevo, Fase 0 de plan_paneles_vectoriales_v3.md): CatalogoSimbolosConectorListado(VentanaListado) + _DialogoSimboloConector(Gtk.Dialog), mismo patrón que TiposCableListado/_DialogoTipoCable de catalogos_basicos_ui.py. El diálogo tiene combo buscable de tipo_conector (_searchable_combo + Modelo.devolver_tipos_conectores, ya existente), campos de tamaño relativo/viewBox/color, un Gtk.TextView para el fragmento SVG y una _VistaPreviaSimbolo (DrawingArea de 64x64 que redibuja en vivo con _crear_handle_simbolo/_dibujar_simbolo_conector) para no tener que guardar a ciegas. _validar_fragmento_svg() valida XML antes de aceptar (xml.etree envuelto en un <svg> de prueba), bloqueando el cierre del diálogo con mostrar_error si el fragmento no es válido o el tamaño relativo no es > 0. Alta rechaza duplicados por id_tipo_conector (un símbolo por tipo, columna UNIQUE) con mensaje explícito en vez de fallar por la constraint.
2026-09-07T19:30 cabledoc.py: import de catalogo_simbolo_conector_ui.CatalogoSimbolosConectorListado, nuevo ítem "🔷 Símbolos de Conector (vectorial)" en el menú Catálogos (junto a "Tipos de Conector") con handler _abrir_simbolos_conector (mismo patrón self._abrir_ventana que el resto). Agregadas al arranque las llamadas Modelo.asegurar_tablas_catalogo_simbolos() y Modelo.asegurar_columnas_dimensiones() junto al resto de los asegurar_tablas_*/asegurar_columnas_* ya existentes. APP_VERSION → 1.20260907193000.
2026-09-07T19:30 imagen_conectores_ui.py (Fase 1 de plan_paneles_vectoriales_v3.md): ImagenConectoresYCables._cargar() extiende el SELECT de conectores con c.id_tipo_conector e i.id_imagen (agregados AL FINAL de la lista de columnas, índices 5 y 6, para no correr los índices 0-4 que ya se leían) y guarda id_tipo_conector en cada marcador + el id_imagen de la imagen predominante en self._id_imagen_actual. Nuevo método _preparar_simbolos_conector(pb), llamado una sola vez tras cargar el pixbuf (no en cada redibujo): activo sólo si isinstance(pb, _ImagenSVG) (regla de activación §4 del plan — el fondo tiene que ser SVG), resuelve self._mm_por_pixel vía Modelo.resolver_mm_por_pixel("equipo", ...) y precarga un Rsvg.Handle por cada id_tipo_conector presente entre los marcadores vía Modelo.obtener_simbolos_conector + _crear_handle_simbolo. _dibujar_overlay(): por cada marcador, si self._simbolos_activos y hay handle para su tipo, calcula el radio con Modelo.calcular_radio_simbolo_px(..., radio_default_px=self.MARCADOR/2) y dibuja el símbolo con _dibujar_simbolo_conector (con aro de resaltado y número debajo, mismo criterio visual que el marcador genérico); si el símbolo se dibujó OK hace `continue` y NO cae al marcador cuadrado genérico. Si _dibujar_simbolo_conector devuelve False (handle roto, radio inválido, etc.) el código sigue de largo al bloque del marcador genérico sin cambios — ese bloque queda intacto, es el fallback. Fotos ya documentadas (fondo no-SVG) no cambian de aspecto en absoluto.
2026-09-07T19:30 editor_masivo_conectores_ui.py (Fase 1 de plan_paneles_vectoriales_v3.md): mismo mecanismo que imagen_conectores_ui.py pero en la clase base compartida EditorMasivoConectoresBase, para que beneficie a las dos subclases (equipo real y molde de catálogo) de una sola vez. Contrato de _cargar_datos() extendido con un 8vo elemento opcional id_tipo_conector (documentado en el docstring de la clase); nuevo atributo de clase _TABLA_DIMENSIONES (None en la base, "equipo" en EditorMasivoConectoresImagen, "equipo_catalogo" en EditorMasivoConectoresCatalogo) que le dice a _preparar_simbolos_conector(pb, id_imagen) de qué tabla sacar ancho_mm cuando no hay medición manual en la imagen (fuente 2 de la cascada de Modelo.resolver_mm_por_pixel). _cargar() calcula el id_imagen de la imagen predominante (antes sólo se guardaba el path) buscando la primera fila de `cons` cuyo path coincida. _dibujar_overlay(): mismo criterio de fallback que imagen_conectores_ui.py — si hay símbolo activo para el tipo del conector se dibuja con forma real (radio vía Modelo.calcular_radio_simbolo_px(..., radio_default_px=self.R), sin multiplicar por zoom, igual que ya hacía el círculo genérico en esta clase) y se hace `continue`; si no, cae intacto al círculo genérico + sombra + etiqueta que ya existía. EditorMasivoConectoresImagen._cargar_datos y EditorMasivoConectoresCatalogo._cargar_datos actualizados para agregar c.id_tipo_conector/r[3] al final de la tupla que devuelven, sin tocar los 7 elementos existentes que ya consumían otros lugares.
2026-09-07T19:30 schema_db.sql: agregadas ancho_mm/alto_mm/profundidad_mm (REAL) a las definiciones CREATE TABLE de equipo/equipo_catalogo/frame/frame_catalogo, mm_por_pixel (REAL) a imagen, y la tabla nueva catalogo_simbolo_conector — para que una instalación fresca (sin database/db.db previo) ya tenga todo sin depender de que corran las migraciones idempotentes de modelo.py (que igual corren al arranque y son no-op sobre un esquema que ya las trae, verificado).
2026-09-07T19:30 Validación de la Fase 0 y Fase 1 de plan_paneles_vectoriales_v3.md: (1) ast.parse sobre los 7 archivos tocados/creados: OK; (2) pyflakes comparado contra el HEAD previo (git show HEAD:archivo.py | pyflakes) para cabledoc.py/imagen_conectores_ui.py/editor_masivo_conectores_ui.py: cero advertencias nuevas (mismas preexistentes, sólo desplazadas de línea); modelo.py/pantallas_comunes.py/catalogo_simbolo_conector_ui.py: sin ninguna advertencia; (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, gir1.2-rsvg-2.0, gir1.2-gdkpixbuf-2.0, python3-gi-cairo, xvfb, pyflakes; (4) smoke tests funcionales (sin levantar ventanas GTK completas, ver nota abajo) contra una base SQLite generada desde el schema_db.sql de esta misma entrega: alta/listado/lookup masivo de catalogo_simbolo_conector; obtener_dimensiones/actualizar_dimensiones incluyendo el caso adversarial de tabla no-whitelisteada ('DROP TABLE equipo;--', confirmado que no ejecuta nada y la tabla sigue con sus filas); las 3 fuentes de resolver_mm_por_pixel en cascada (medición manual gana a ancho_mm, que gana a None); calcular_radio_simbolo_px con tamano_relativo 1.0 vs 0.75 confirmando que el segundo da un radio menor; _crear_handle_simbolo/_dibujar_simbolo_conector con Rsvg/Cairo reales bajo Xvfb (fragmento válido dibuja OK, fragmento roto devuelve None/False sin excepción); EditorMasivoConectoresImagen._cargar_datos y EditorMasivoConectoresCatalogo._cargar_datos ejercitados con datos de fixture (marca/tipo_equipo/tipo_conector/imagen/equipo/equipo_catalogo/conector_catalogo) confirmando que el id_tipo_conector llega como 8vo elemento de la tupla en ambas variantes; carga completa de schema_db.sql en una base nueva confirmando que las 5 tablas ya traen las columnas nuevas sin necesidad de migración. (5) **Pendiente para Papi — smoke test visual con GTK real**: por indicación explícita se dejó el smoke test visual (instanciar las ventanas completas, abrir el diálogo de símbolo con .run(), verificar el render del símbolo sobre una imagen SVG real dentro de ImagenConectoresYCables/EditorMasivoConectoresBase con datos reales de database/db.db) para correr en la máquina real antes de dar por cerrada esta entrega — Xvfb se cayó de forma intermitente en este sandbox entre llamadas de herramienta y no se insistió más allá de la validación funcional sin ventana ya descripta arriba. Tampoco se implementó todavía la herramienta "Medir en la imagen" (§3.4 del plan) ni el copiado automático de ancho_mm/alto_mm/profundidad_mm al instanciar un equipo desde un molde de catálogo — ver PROGRESS.md para el detalle de lo que queda para la próxima sesión.
2026-09-06T04:00 modelo.py (Fase 7 de plan_desarrollo_ubicacion_fisica_planos.md — "Equipos sueltos, módulos de frame y herencia de ubicación"): agregada eq.es_modulo_de_frame al final de devolver_equipo (columna 18, sin tocar 0-17) — la columna y actualizar_es_modulo_de_frame ya existían desde la Fase 1 sin consumidor en la UI. Extendida devolver_equipo_no_rack_sala(id_) — agrega s.id_plano y en.tipo_montaje al final (columnas 5 y 6), mismo criterio que devolver_rack_por_sala en la Fase 5. alta_equipo_no_rack_sala/modificacion_equipo_no_rack_sala ganan el parámetro opcional tipo_montaje='PISO' (modificacion_equipo_no_rack_sala no toca x_pct/y_pct, esos siguen editándose aparte vía actualizar_posicion_equipo_no_rack_sala, que ya existía desde la Fase 1 y ya aceptaba tipo_montaje). Agregada devolver_equiponoraqueable_de_plano(id_plano) (todos los equiponoraqueable_por_sala de salas de un plano, con o sin punto todavía — mismo criterio que devolver_racks_por_sala_de_plano/devolver_muebles_de_plano, para el selector "Equipo suelto:" de VistaPlanoInteractivo). Agregada devolver_equipos_de_rack_con_modulos(id_rack) (equipos montados directo en el rack + módulos instalados en slots de los frames que ese rack tiene rackeados, con el origen de cada uno — 'directo' o el nombre del frame — para el clic sobre un rack en el visor). devolver_ubicacion_fisica_de_equipo/devolver_contenido_plano (con su clave equipos_sueltos) ya resolvían toda la cadena rack→módulo de frame→mueble→suelto desde la Fase 1, sin consumidor real hasta esta entrega.
2026-09-06T04:00 equipos_ui.py (Fase 7): _DialogoEquipo suma el checkbox "Es módulo de frame (requiere estar instalado en un frame para tener ubicación física)" al final de la pestaña Datos, con tooltip explicando la exclusión mutua con equipo suelto/mueble. Se carga desde devolver_equipo[18] al editar. Al guardar se persiste vía Modelo.actualizar_es_modulo_de_frame en vez de sumarse como parámetro de alta_equipo/modificacion_equipo (mismo criterio de campo desacoplado que ya usa chk_equipo_critico, para no tocar la firma de esas dos funciones); el alta de equipo nuevo pasa de Modelo.alta_equipo a Modelo.alta_equipo_retorna_id para tener el id disponible y poder marcarlo módulo de frame en el mismo guardado si corresponde.
2026-09-06T04:00 racks_salas_ui.py (Fase 7): _DialogoEquipoNoRackSala suma selector "Tipo de montaje" (Piso/Pared, sólo cambia el ícono con el que se dibuja el punto en el overlay — no hay geometría de pared), filtra el buscador de equipo con EquiposListado(excluir_modulos_de_frame=True) (ya soportado desde la Fase 6) para no poder asignar como suelto un equipo marcado módulo de frame, y suma etiqueta de estado + botón "📍 Ubicar en el plano" (habilitado sólo si la asignación ya está guardada y su sala tiene plano) que abre VistaPlanoInteractivo(id_plano_de_sala, id_equiponoraqueable_foco=self.id_) — mismo patrón que "📍 Ubicar en el plano" de _DialogoRackPorSala en la Fase 5.
2026-09-06T04:00 planos_ui.py (Fase 7): VistaPlanoInteractivo suma overlay de equipos sueltos — círculo violeta (COLOR_SUELTO) por cada equiponoraqueable_por_sala con punto ya cargado, borde sólido si tipo_montaje=PISO (ícono 🖴) o punteado si PARED (ícono 🧱); nueva cuarta barra inferior con combo "Equipo suelto:" (Modelo.devolver_equiponoraqueable_de_plano) + botón "📍 Ubicar/mover este equipo suelto en el plano" (_ubicar_equipo_suelto), que reutiliza el mismo mecanismo de punto simple del rack (CoordenadasImagenSeleccion solo_xy=True vía abrir_coords_imagen) pero conserva el tipo_montaje ya cargado en self._equipo_suelto_datos — este botón sólo mueve el punto, no lo cambia. Nuevo parámetro id_equiponoraqueable_foco para preseleccionar un equipo suelto al abrir, usado por _DialogoEquipoNoRackSala."📍 Ubicar en el plano" (racks_salas_ui.py). Suma también hit-testing simple sobre los cuadrados de rack: conectado button-press-event sobre self._viz.da (_on_click_overlay), que convierte el clic a coordenadas de imagen vía self._viz.w2i y busca el rack más cercano dentro de una tolerancia de ~20px de pantalla ajustada por zoom; al encontrar uno, lo marca en self._rack_resaltado (dibujado con un halo amarillo — COLOR_RESALTADO — alrededor del cuadrado en el próximo redibujado) y lista, en una quinta etiqueta bajo el visor, los equipos que trae puestos ese rack (Modelo.devolver_equipos_de_rack_con_modulos: directos + módulos de sus frames, con el nombre del frame como origen de cada módulo). Docstring del módulo actualizado documentando el cierre de la Fase 7.
2026-09-06T04:00 Validación de la Fase 7 de plan_desarrollo_ubicacion_fisica_planos.md (mismo checklist que las fases anteriores del plan): (1) ast.parse + py_compile sobre los 4 archivos tocados (modelo.py, equipos_ui.py, racks_salas_ui.py, planos_ui.py): OK; (2) pyflakes comparado contra el HEAD previo a este cambio (git stash): cero advertencias nuevas en los 4 archivos (las 7 preexistentes de equipos_ui.py — imports de re/subprocess/MANUALES_DIR sin usar a nivel de módulo, redefinidos localmente en otros métodos — siguen ahí sólo con el número de línea desplazado); (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, python3-gi-cairo, gir1.2-rsvg-2.0, xvfb y pip graphqlite; (4) smoke test funcional bajo Xvfb contra una base SQLite generada desde schema_db.sql + Modelo.asegurar_tablas_plano() (corrida dos veces sin error, confirma idempotencia), con fixture de 1 plano/1 sala con contorno/1 rack ubicado/1 equipo directo en el rack/1 frame rackeado con 1 módulo en un slot (equipo marcado es_modulo_de_frame)/2 equipos sueltos (uno PISO, otro PARED): confirmado que devolver_ubicacion_fisica_de_equipo resuelve el equipo directo y el módulo de frame al mismo punto del rack, y el suelto a su propio punto; devolver_contenido_plano trae 1 rack + 2 equipos_sueltos; devolver_equipos_de_rack_con_modulos trae los 2 equipos esperados con sus orígenes ('directo' y el nombre del frame); VistaPlanoInteractivo instanciada con la imagen de fixture cargada, overlay dibujado sin excepciones contra un cairo.Context real (incluidos los 2 círculos de equipo suelto y el cuadrado del rack); clic simulado con FakeEvent(x,y) sobre el punto exacto del rack (convertido con self._viz.i2w) resaltó el rack correcto (self._rack_resaltado) y la etiqueta mostró los 2 equipos con sus orígenes; _DialogoEquipoNoRackSala en modo editar sobre el equipo PARED confirmó c_tipo_montaje en "PARED", id_plano_de_sala resuelto y btn_ubicar habilitado, y en modo alta confirmó btn_ubicar deshabilitado; _DialogoEquipo confirmó chk_es_modulo_frame activo para el equipo módulo y apagado para el equipo directo. Pendiente arrastrada (acumulada desde la Fase 2): correr el smoke test visual completo contra el database/db.db real de Fede/Papi, en particular ubicar equipos sueltos reales sobre la imagen real de un plano y confirmar visualmente los dos íconos (Piso/Pared) y el clic sobre un rack con equipos reales montados. Con esta entrega se completan las 7 fases centrales de plan_desarrollo_ubicacion_fisica_planos.md (queda pendiente sólo la Fase 8, "limpieza de hardcodes" — sacar imagen/coordenada_x/coordenada_y de equipo — y la Fase 9, "Pulido").
2026-09-06T00:30 modelo.py (Fase 6 de plan_desarrollo_ubicacion_fisica_planos.md — "Muebles"): la capa de modelo (tablas mueble/equipo_sobre_mueble, alta_mueble_retorna_id, modificacion_mueble, eliminar_mueble, devolver_mueble, devolver_muebles_de_sala, asignar_equipo_a_mueble, quitar_equipo_de_mueble, devolver_equipos_de_mueble, resolución de posición absoluta en devolver_ubicacion_fisica_de_equipo, conversión rect px↔% con _rect_px_a_pct/_rect_pct_a_px/_pct_rect_o_none/_px_rect_o_crudo) ya existía completa desde la Fase 1 sin consumidor — esta entrega es mayormente UI. Cambios reales: extendida devolver_mueble(id_mueble) — agrega s.id_plano al final (columna 8, sin tocar 0-7), mismo criterio que devolver_rack_por_sala en la Fase 5. Agregada devolver_muebles_de_plano(id_plano) (todos los muebles de salas de un plano, con o sin rectángulo todavía — para el selector "Mueble:" de VistaPlanoInteractivo). Agregada devolver_todos_los_muebles() (listado plano con nombre de sala resuelto, para MueblesListado). Extendida devolver_contenido_plano: cada mueble ahora trae anidada su lista de equipos (id_equipo, nombre, x_pct_relativo, y_pct_relativo) via un query adicional por mueble — no se batchea a nivel de plano como racks/sueltos porque la cardinalidad de muebles por sala es chica (ver comentario en el código).
2026-09-06T00:30 equipos_ui.py (Fase 6): EquiposListado gana el parámetro opcional excluir_modulos_de_frame=False (default False, no cambia el comportamiento de los llamadores existentes) — cuando True, filtra de la lista los equipos con equipo.es_modulo_de_frame=1 antes de poblar la tabla. Usado por _DialogoMueble."➕ Agregar equipo" (planos_ui.py) para no poder asignar a un mueble un equipo que en realidad va instalado en un frame. El checkbox en la UI para marcar es_modulo_de_frame llega recién en la Fase 7 — la columna y el filtro ya funcionan igual, sólo falta la forma de tildarlo desde _DialogoEquipo.
2026-09-06T00:30 planos_ui.py (Fase 6): nuevas MueblesListado + _DialogoMueble — alta/edición de mueble (nombre, sala, tipo) con botón "▭ Definir rectángulo en el plano" (abre VistaPlanoInteractivo enfocado en el mueble, habilitado sólo si el mueble ya está guardado Y su sala tiene plano — mismo patrón que "📍 Ubicar en el plano" de _DialogoRackPorSala en la Fase 5) y gestión de equipos asignados (lista + "➕ Agregar equipo" con excluir_modulos_de_frame=True y posición relativa inicial 50/50, "➖ Quitar equipo", "📍 Ubicar dentro del mueble" — reutiliza abrir_coords_imagen en modo punto sobre la imagen completa del plano, convirtiendo el resultado a % relativo al rectángulo del mueble, recortado a 0-100 si el clic cae afuera). VistaPlanoInteractivo suma overlay de muebles: rectángulo verde (COLOR_MUEBLE) con los equipos que contiene dibujados adentro como puntos, nuevo combo "Mueble:" (Modelo.devolver_muebles_de_plano) + botón "▭ Ubicar/redimensionar este mueble en el plano" reutilizando el modo rectángulo ya existente de CoordenadasImagenSeleccion (solo_xy=False) — sin cambios en imagen_conectores_ui.py, tal como preveía el plan. Nuevo parámetro id_mueble_foco para preseleccionar un mueble al abrir. Reincorporado el import `math` (se había sacado en el ajuste de racks-a-cuadrados; ahora hace falta de nuevo para el punto de equipo dentro del mueble).
2026-09-06T00:30 cabledoc.py (Fase 6): nueva entrada de menú "🪑 Muebles" en Infraestructura (después de "🗺 Planos") → MueblesListado. Import de MueblesListado agregado al bloque ya existente de planos_ui.
2026-09-05T23:05 planos_ui.py (ajuste sobre la Fase 5, a pedido de Pepe): el rack en el overlay pasa de dibujarse como círculo (cr.arc) a cuadrado (cr.rectangle) — mismo color (COLOR_RACK, naranja), mismo criterio de tamaño relativo al zoom (antes radio = max(6, 9*zoom); ahora lado = max(12, 18*zoom), equivalente al diámetro que tenía el círculo), mismo texto con el nombre del rack al lado. Sacado el import `math` (ya no se usa: cr.arc necesitaba math.pi, cr.rectangle no).
2026-09-05T22:30 modelo.py (Fase 5 de plan_desarrollo_ubicacion_fisica_planos.md — "Overlay de racks (puntos)"): extendida devolver_rack_por_sala(id_) — agrega s.id_plano al final (columna 5, sin tocar las columnas 0-4 que ya consumía _DialogoRackPorSala), necesaria para que ese diálogo sepa si la sala de una asignación ya tiene plano asignado. Agregada devolver_racks_por_sala_de_plano(id_plano) (todos los rack_por_sala de salas de un plano, con o sin punto x_pct/y_pct todavía — a diferencia de devolver_contenido_plano, que dentro de cada sala sólo trae los racks con punto ya cargado, pensado para el overlay de sólo lectura — pensada para poblar el selector "Rack:" de VistaPlanoInteractivo en modo editar). Corregido devolver_contenido_plano: se quitó el filtro `AND poligono IS NOT NULL` sobre `sala` — antes excluía por completo del resultado a cualquier sala sin contorno todavía, aunque ya tuviera racks/muebles/equipos sueltos ubicados; no se había notado porque hasta esta entrega el único consumidor (Fase 4) sólo leía el polígono. actualizar_posicion_rack_por_sala ya existía desde la Fase 1 sin consumidor — se reutiliza sin cambios.
2026-09-05T22:30 planos_ui.py (Fase 5): VistaPlanoInteractivo suma overlay de puntos de rack — en _dibujar_overlay, por cada sala del plano (con o sin contorno dibujado: el bloque de contorno ahora es condicional en vez de cortar con `continue`, ver fix de Modelo.devolver_contenido_plano) se dibuja un punto naranja (COLOR_RACK) + nombre por cada rack con x_pct/y_pct ya cargado. Nuevo combo "Rack:" (segunda barra inferior) poblado con Modelo.devolver_racks_por_sala_de_plano (TODOS los rack_por_sala del plano, tengan o no punto — mismo criterio que el combo de Sala) + botón "📍 Ubicar/mover este rack en el plano" (_ubicar_rack): a diferencia del contorno de sala (polígono dibujado a mano), reutiliza CoordenadasImagenSeleccion en su modo solo_xy=True ya existente (mecanismo histórico de conectores/slots) vía abrir_coords_imagen, precargado con la posición actual convertida a píxeles (Modelo._px_punto_o_crudo, que nunca levanta) y, al aceptar, la guarda convertida de vuelta a porcentaje (Modelo._punto_px_a_pct) mediante Modelo.actualizar_posicion_rack_por_sala. Constructor acepta id_rack_x_sala_foco opcional para preseleccionar un rack en el combo al abrir, usado por _DialogoRackPorSala."📍 Ubicar en el plano" (racks_salas_ui.py). Import de nivel de módulo agregado: `import math` (radio del punto/centro del arco).
2026-09-05T22:30 racks_salas_ui.py (Fase 5): _DialogoRackPorSala gana etiqueta de estado + botón "📍 Ubicar en el plano", habilitado sólo cuando la asignación ya está guardada (self.id_) y la sala de esa asignación ya tiene un plano asignado (sala.id_plano, vía la columna nueva de devolver_rack_por_sala) — abre VistaPlanoInteractivo(id_plano_de_sala, id_rack_x_sala_foco=self.id_) (planos_ui.py, import diferido dentro del método, mismo patrón que _DialogoSala._editar_contorno). Fix incidental (no relacionado con la Fase 5, encontrado al ejercitar el diálogo bajo GTK real para validar el botón nuevo): a esta clase le faltaba el método run_and_destroy — RackPorSalaListado.nuevo()/.editar() ya lo llamaban desde antes de esta entrega, lo que levantaba AttributeError al abrir "Nueva"/"Editar" asignación Rack-Sala desde el menú (bug preexistente en main, nunca se había notado por falta de smoke test bajo GTK real de este diálogo puntual); agregado con el mismo patrón que el resto de los diálogos con guardado vía señal "response".
2026-09-05T20:00 modelo.py: extendida devolver_sala(id_sala) — ahora devuelve id_sala, nombre, id_plano, poligono (antes sólo id_sala, nombre); verificado que sus dos únicos consumidores (racks_salas_ui.py, panel_arbol_ui.py) sólo leían las columnas 0/1, así que la extensión no rompe nada existente. Agregadas devolver_salas_de_plano(id_plano) (todas las salas de un plano, con o sin contorno, a diferencia de devolver_contenido_plano que sólo trae las que ya tienen poligono — pensada para poblar el selector "Sala:" de VistaPlanoInteractivo en modo editar) y alta_sala_retorna_id(nombre) (igual a alta_sala pero devuelve el lastrowid, mismo patrón que alta_plano_retorna_id/alta_mueble_retorna_id — necesaria para poder asignarle un plano en el mismo alta sin ir a buscarla de nuevo).
2026-09-05T20:00 racks_salas_ui.py (Fase 4 de plan_desarrollo_ubicacion_fisica_planos.md — "Overlay de salas en el plano"): nueva clase _DialogoSala, que reemplaza el uso del genérico DialogoNombre en SalasListado.nuevo()/.editar(). Agrega el selector de Plano (botón "…" que abre PlanosListado en modo_seleccion, import diferido `from cabledoc import PlanosListado`, mismo patrón ya usado en el resto del archivo para referencias cruzadas), una etiqueta de estado del contorno ("sin definir" / "Contorno: N vértices" / mensajes de guía cuando falta sala o plano) y el botón "🗺 Editar contorno en el plano", que abre VistaPlanoInteractivo (planos_ui.py, import diferido) enfocado en la sala actual. Reglas: el botón de contorno queda deshabilitado si la sala todavía no fue guardada (no hay id_sala) o si no tiene plano asignado; si se cambia el plano de una sala que ya tenía contorno, el contorno se descarta (self._poligono = None) porque estaba dibujado sobre la imagen del plano anterior — mismo criterio que documenta plan_desarrollo_ubicacion_fisica_planos.md §3.2 para el caso de reemplazar la imagen del plano. run_and_destroy() persiste nombre + id_plano/poligono en un solo alta/modificación: alta nueva usa alta_sala_retorna_id + actualizar_ubicacion_sala si se eligió plano (siempre con poligono=None, ya que el contorno recién se puede dibujar una vez que la sala existe); edición usa modificacion_sala + actualizar_ubicacion_sala. Se quitó el import de DialogoNombre, que quedó sin uso en este archivo.
2026-09-05T20:00 planos_ui.py (Fase 4 de plan_desarrollo_ubicacion_fisica_planos.md): nueva clase VistaPlanoInteractivo(Gtk.Dialog). Modo navegar (siempre activo): overlay Cairo propio (_dibujar_overlay) sobre _ImagenZoom que dibuja el contorno de cada sala del plano con poligono ya cargado (vía Modelo.devolver_contenido_plano, que ya traía todo lo necesario desde la Fase 1 aunque hasta ahora no tenía consumidor), con relleno azul tenue + borde sólido + nombre de la sala centrado en el centroide del polígono — a propósito no dibuja todavía racks/muebles/equipos sueltos (esos overlays llegan en las Fases 5 a 7; devolver_contenido_plano ya los trae, pero agregarlos ahora se saldría del criterio de cierre de esta fase). Modo editar: un combo "Sala:" poblado con Modelo.devolver_salas_de_plano (TODAS las salas del plano, tengan o no contorno todavía — a diferencia del overlay de sólo lectura) + botón "✏ Editar contorno de esta sala" que reutiliza abrir_coords_imagen(..., modo_poligono=True) de la Fase 3 (imagen_conectores_ui.py), precargando el contorno actual (convertido de % a píxeles de imagen vía Modelo._punto_pct_a_px) y, si el usuario cierra el polígono con 3+ vértices, lo guarda de vuelta convertido a % (Modelo._punto_px_a_pct) mediante Modelo.actualizar_ubicacion_sala. Si el usuario cancela el diálogo sin cerrar el polígono, se avisa con mostrar_error y no se sobreescribe el contorno existente. Constructor acepta id_sala_foco opcional para preseleccionar una sala en el combo al abrir, usado por _DialogoSala."Editar contorno en el plano" (racks_salas_ui.py) para no obligar a rebuscarla. Import de nivel de módulo agregado: `from imagen_conectores_ui import abrir_coords_imagen`; `import json`; `from modelo import ... DimensionesImagenError` agregado a la import ya existente de Modelo/IMG_DIR. Docstring del módulo actualizado (ya no dice "sin overlay todavía").
2026-09-05T20:00 cabledoc.py: agregado `_DialogoSala` al bloque de reexport `from racks_salas_ui import (...)` (mismo patrón que el resto de los diálogos "_Dialogo*" del archivo) — necesario porque panel_arbol_ui.py lo consume vía `from cabledoc import _DialogoSala`. APP_VERSION → 1.20260905200000.
2026-09-05T20:00 panel_arbol_ui.py: el dispatcher de doble clic sobre un nodo "sala" (_on_row_activated) reemplaza el genérico DialogoNombre por _DialogoSala (import diferido agregado junto al resto de _DialogoEquipo/_DialogoRack/etc.), por consistencia — antes de esta entrega, editar una sala desde el árbol abría un diálogo distinto (sólo nombre) al que abría SalasListado (nombre + plano + contorno), lo cual iba a confundir apenas se empezara a usar el plano. Se quitó el import de DialogoNombre, que quedó sin uso en este archivo.
2026-09-05T20:00 Validación de la Fase 4 de plan_desarrollo_ubicacion_fisica_planos.md (mismo checklist que las fases anteriores del plan): (1) ast.parse + py_compile sobre los 5 archivos tocados (modelo.py, racks_salas_ui.py, planos_ui.py, cabledoc.py, panel_arbol_ui.py): OK; (2) pyflakes comparado sentencia por sentencia contra el HEAD previo a este cambio (git stash): modelo.py/racks_salas_ui.py/planos_ui.py sin ninguna advertencia; cabledoc.py y panel_arbol_ui.py sin advertencias nuevas más allá del reexport esperado de _DialogoSala y el desplazamiento de línea de las preexistentes (sys sin usar, variable local 'e' sin usar, 3 f-strings sin placeholder en panel_arbol_ui.py) — cero F821 nuevos; (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, python3-gi-cairo, gir1.2-rsvg-2.0, xvfb y pip graphqlite; (4) smoke test funcional bajo Xvfb contra una base SQLite generada desde schema_db.sql + Modelo.asegurar_tablas_plano() (corrida dos veces sin error, confirma idempotencia) con un plano de fixture (imagen SVG de 1000x800 con dos rectángulos dibujados) y 3 salas de fixture (dos asignadas al plano, una sin plano a propósito): confirmada identidad de objeto cabledoc._DialogoSala is racks_salas_ui._DialogoSala; _DialogoSala habilita/deshabilita correctamente el botón de contorno según haya id_sala/id_plano (los 3 casos: sala existente con plano, sala existente sin plano, sala nueva sin guardar); VistaPlanoInteractivo carga el combo sólo con las salas del plano abierto (no la sala sin plano), id_sala_foco preselecciona correctamente, y el overlay (_dibujar_overlay) corrió sin excepciones contra un cairo.Context real tanto con 0 como con 2 salas ya con contorno dibujado; flujo interactivo end-to-end con CoordenadasImagenSeleccion real y clics simulados con FakeEvent(x,y) (mismo patrón que la validación de la Fase 3): se dibujó y cerró un polígono de 4 vértices sobre la sala B, se confirmó el x_pct/y_pct persistido en sala.poligono, se confirmó que el combo deja de mostrar "(sin contorno)" para esa sala tras guardar, y se confirmó que un intento de guardar un polígono sin cerrar NO sobreescribe el contorno existente (con mostrar_error interceptado vía monkeypatch de Gtk.MessageDialog.run, mismo criterio de mock que el resto de la suite); (5) smoke test de arranque adicional: VentanaPrincipal completa instanciada bajo Xvfb contra una base vacía con Modelo.asegurar_tablas_plano() ya migrado, sin excepciones. Pendiente arrastrada (acumulada desde entregas anteriores del plan): correr el smoke test visual completo contra el database/db.db real de Fede, en particular dibujar el contorno de salas reales sobre la imagen real de un plano (criterio de cierre original de la Fase 4) y confirmar que persiste al reabrir el visor.
2026-09-05T17:30 imagen_conectores_ui.py (Fase 3 de plan_desarrollo_ubicacion_fisica_planos.md — "Selector de polígono sobre imagen"): CoordenadasImagenSeleccion gana un tercer modo, modo_poligono=True, agregado aparte de solo_xy (que sigue significando punto vs. rectángulo, sin tocarse) porque el polígono es ortogonal a esa distinción. Interacción sobre la imagen: clic en área vacía agrega un vértice al final; clic-y-arrastre sobre un vértice existente lo mueve (hit-test con el nuevo RADIO_VERTICE=16, mismo criterio de radio en píxeles de imagen que ya usaba MARCADOR/2 para los marcadores existentes); clic sobre el primer vértice, con 3 o más ya cargados, cierra el polígono. Overlay Cairo propio (_dibujar_overlay_poligono): borde sólido entre vértices consecutivos, segmento de cierre punteado mientras no está cerrado, relleno tenue una vez cerrado, vértice 1 resaltado en otro color porque es el que hay que tocar para cerrar. Resultado público en self.vertices (lista de (x, y) en píxeles de imagen, mismo sistema de coordenadas que x/y en los otros dos modos — la conversión a x_pct/y_pct queda para el llamador, vía Modelo._punto_px_a_pct, igual que ya hacen los llamadores existentes de solo_xy=True) y self.cerrado (bool). Se puede precargar un polígono guardado con vertices=[...]: si trae 3+ vértices arranca cerrado (un polígono persistido en sala.poligono ya está cerrado por definición), con botón "🔓 Reabrir para seguir editando" para volver a editarlo sin perder los vértices; también se agregaron "↶ Deshacer último vértice" y "🗑 Reiniciar polígono". do_response() ahora hace un no-op sobre self.vertices/self.cerrado en modo polígono (no hay campos de texto que leer, ya quedan al día con cada clic/arrastre). abrir_coords_imagen() extendida con modo_poligono/vertices, devuelve {"vertices":..., "cerrado":...} en ese modo. Sin cambios de comportamiento en los modos punto/rectángulo existentes.

2026-09-05T17:30 (validación de la Fase 3, mismo checklist que las fases anteriores del plan): (1) ast.parse + py_compile sobre imagen_conectores_ui.py: OK; (2) pyflakes comparado contra el HEAD previo a este cambio: cero advertencias nuevas (las dos preexistentes, 'os' y 'pantallas_comunes._pixbuf_from_name' importados sin usar, siguen ahí solo con el número de línea desplazado); (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, python3-gi-cairo, gir1.2-rsvg-2.0 y xvfb; (4) smoke test funcional bajo Xvfb con GTK real (Gtk.Dialog real, self._viz.da real, eventos de mouse simulados a mano con objetos FakeEvent(x,y) llamando directo a _on_press/_on_motion/_on_release, mismo patrón de mocks ya usado en la Entrega 9 del refactor de cabledoc.py): se dibujó un polígono de 5 vértices con clics, se movió uno arrastrándolo, se cerró haciendo clic sobre el vértice 1, se instanció un segundo diálogo precargando esos mismos vértices (confirmado que reabre con cerrado=True), se lo reabrió con el botón para seguir editando y se le agregó un sexto vértice, y se probaron deshacer/reiniciar — los 9 pasos confirmaron el criterio de cierre de la Fase 3 palabra por palabra ("dibujar un polígono de 5+ vértices, cerrarlo, reabrir el diálogo y confirmar que carga los vértices guardados para seguir editando"); (5) regresión de los modos existentes verificada aparte con el mismo self._viz.da real: modo punto (solo_xy=True) y modo rectángulo (solo_xy=False) probados con clic/arrastre tal como ya funcionaban, sin cambios de resultado.

2026-09-05T16:00 planos_ui.py: creado (Fase 2 de plan_desarrollo_ubicacion_fisica_planos.md, "Catálogo de Planos, sin overlay todavía"). Contiene PlanosListado (columnas ID/Nombre/Imagen/Orden, sobre Modelo.devolver_todos_los_planos()) y _DialogoPlano (alta/edición: nombre, imagen vía botón "📂 Explorar" que copia el archivo elegido a IMG_DIR con el mismo criterio que _DialogoImagen de catalogos_basicos_ui.py, y orden). A diferencia de _DialogoImagen, acá no se elige una fila ya existente de la tabla `imagen` vía ImagenesListado: Modelo.alta_plano_retorna_id/modificacion_plano (Fase 1, ya en main) gestionan su propia fila de `imagen` 1 a 1 con el plano, así que el diálogo solo pasa el path de archivo elegido. Nuevo dominio en su propio módulo desde el arranque (mismo criterio de separación que racks_salas_ui.py/frames_slots_ui.py), no un *move* desde cabledoc.py — este archivo va a crecer en las Fases 3 a 7 con VistaPlanoInteractivo, MueblesListado y _DialogoMueble.
2026-09-05T16:00 cabledoc.py: agregado `from planos_ui import (PlanosListado, _DialogoPlano)` inmediatamente después del bloque de reexport de racks_salas_ui.py; nuevo ítem de menú "🗺 Planos" al final de Infraestructura (separado por un divisor de "🖼 Vista gráfica de rack…"); nuevo handler `_abrir_planos` junto a `_abrir_equipos_no_rack_sala`, que abre PlanosListado con `.run_and_destroy()` (mismo patrón que `_abrir_salas`/`_abrir_rack_por_sala`/`_abrir_equipos_no_rack_sala`, no `_abrir_ventana()` porque PlanosListado no necesita argumentos extra). APP_VERSION → 1.20260905160000.
2026-09-05T16:00 Validación de la Fase 2 de plan_desarrollo_ubicacion_fisica_planos.md (mismo checklist que las entregas del refactor de cabledoc.py, adaptado a un archivo nuevo en vez de un *move*): (1) ast.parse + py_compile sobre planos_ui.py y cabledoc.py: OK; (2) pyflakes sobre planos_ui.py: sin advertencias; sobre cabledoc.py comparado contra el HEAD previo a este cambio: única advertencia nueva es "planos_ui._DialogoPlano imported but unused" (mismo patrón de reexport ya usado para todos los diálogos "_Dialogo*" del resto de la fachada, ej. _DialogoRack/_DialogoFrame), cero F821 nuevos; (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, python3-gi-cairo, gir1.2-rsvg-2.0 y xvfb; import real bajo Xvfb de cabledoc.py y de planos_ui.py de forma standalone: OK sin excepciones, identidad de objeto confirmada (cabledoc.PlanosListado is planos_ui.PlanosListado); (4) smoke test funcional bajo Xvfb contra una base SQLite generada desde schema_db.sql + Modelo.asegurar_tablas_plano() (repo sin database/db.db versionado en esta sesión): PlanosListado instanciado sin excepciones en base vacía; alta de un plano de fixture vía Modelo.alta_plano_retorna_id("Planta Baja", "plano_planta_baja.svg", 0) — _DialogoPlano abierto en modo edición confirmó e_nombre.get_text()=="Planta Baja", e_imagen.get_text()=="plano_planta_baja.svg", e_orden.get_text()=="0"; Modelo.devolver_todos_los_planos() devolvió la fila esperada; eliminación del plano de fixture y confirmación de que la lista queda vacía. **No se pudo ejercitar el botón "📂 Explorar" con un diálogo GTK real** (Gtk.FileChooserDialog.run() es modal e interactivo, no simulable desde un script headless) — se cubrió por vía alternativa: revisión manual del código confirma que replica línea por línea la lógica ya validada de _DialogoImagen._explorar (catalogos_basicos_ui.py, Entrega 8), incluida la advertencia de imagen no encontrada en IMG_DIR al guardar. Pendiente para Fede: correr el smoke test visual completo (incluido "Explorar" con un SVG real) contra su entorno con database/db.db real, y confirmar el ítem de menú "🗺 Planos" en Infraestructura antes de seguir con la Fase 3 (selector de polígono sobre imagen).
2026-09-04T23:59 catalogo_equipos_ui.py (_DialogoRenombrarConectoresCatalogo, _DialogoCatalogoEquipo._renombrar_conectores): Entrega 10 de plan_refactor_cabledoc.md (cierre del refactor). Movida 1:1 desde cabledoc.py la clase _DialogoRenombrarConectoresCatalogo (único bloque de dominio sin destino desde la Entrega 8, el plan §4 no la contemplaba por no pertenecer al bloque "catálogos básicos"). Se asignó a este archivo, junto a su único consumidor (_DialogoCatalogoEquipo._renombrar_conectores, en el mismo módulo), en vez de conectores_ui.py (dominio de conectores de equipos reales, no de moldes). El import diferido `from cabledoc import _DialogoRenombrarConectoresCatalogo` dentro de _renombrar_conectores se eliminó (ya no hace falta: la clase vive en el mismo módulo).
2026-09-04T23:59 cabledoc.py: la clase _DialogoRenombrarConectoresCatalogo (105 líneas) reemplazada por `from catalogo_equipos_ui import _DialogoRenombrarConectoresCatalogo`, mismo patrón de reexport que el resto de la fachada. Docstring del módulo actualizado documentando el cierre de la Entrega 10: cabledoc.py queda como fachada delgada (VentanaPrincipal + APP_VERSION + helpers sin dominio propio + entry point + reexports de los 12 módulos *_ui.py extraídos en las Entregas 1-10). 1.080 → 996 líneas. Cero cambios de comportamiento. APP_VERSION → 1.20260904235900.
2026-09-04T23:59 Validación de la Entrega 10 (checklist plan_refactor_cabledoc.md §5, cierre): (1) ast.parse + py_compile sobre cabledoc.py y catalogo_equipos_ui.py: OK; (2) pyflakes comparado contra baseline de main: cero F821 nuevos, única advertencia nueva es "catalogo_equipos_ui._DialogoRenombrarConectoresCatalogo imported but unused" en cabledoc.py, consistente con el patrón de reexport ya establecido en las 9 entregas anteriores; catalogo_equipos_ui.py: cero advertencias nuevas en ambos casos; (3) esta sesión sí tuvo acceso de red: se instalaron gir1.2-gtk-3.0, python3-gi-cairo, xvfb y pip graphqlite sin problemas; import real bajo Xvfb de cabledoc.py y de los 17 módulos externos que hacen `from cabledoc import ...`, identidad de objeto confirmada (cabledoc._DialogoRenombrarConectoresCatalogo is catalogo_equipos_ui._DialogoRenombrarConectoresCatalogo); (4) diff byte a byte de la clase movida contra su ubicación original en main: idéntica carácter por carácter salvo la eliminación del import diferido ya innecesario; (5) smoke test funcional bajo Xvfb contra una copia descartable de database/db.db (este repo no traía database/db.db versionado, generada desde schema_db.sql) con 1 tipo_equipo/1 marca/1 tipo_conector/1 equipo_catalogo ("Molde Fixture")/2 conector_catalogo de fixture insertados por SQL directo: _DialogoRenombrarConectoresCatalogo instanciado directamente cargó los 2 conectores-molde de fixture sin excepciones; (6) smoke test de cierre adicional (más allá del checklist estándar de las Entregas 1-9): VentanaPrincipal completa instanciada bajo Xvfb sin excepciones contra la base de fixture, confirmando que el cierre de la fachada no rompió el arranque de la app. Con esta entrega se cierra plan_refactor_cabledoc.md: cabledoc.py pasó de 9.405 líneas originales a 996 (fachada delgada), repartidas en 12 módulos *_ui.py de dominio. Rama: refactor/etapa10-cierre-facade (creada localmente desde main de GitHub, sin commitear — diff entregado para que Fede haga el commit en su terminal).
2026-09-04T01:15 catalogo_equipos_ui.py: creado (Entrega 5 de plan_refactor_cabledoc.md, parte 1/2). Movidas 1:1 desde cabledoc.py: CatalogoEquiposListado, _DialogoConflictosImportacion, _DialogoCatalogoEquipo, _ConectoresCatalogoListado, _DialogoConectorCatalogo, _DialogoInstanciarCatalogo, _DialogoDuplicarMolde (bloque contiguo "Catálogo de equipos (moldes)", líneas 1902-3106 antes de esta entrega, 1.205 líneas). _DialogoDuplicarMolde y _DialogoAltaRapidaCatalogo (ver parte 2/2) eran justamente los dos nombres que la Entrega 3 había dejado pendientes en cabledoc.py por pertenecer a este dominio. Referencias cruzadas a MarcasListado, TiposEquipoListado, ImagenesListado, _DialogoRenombrarConectoresCatalogo, _escribir_json_comprimido, _leer_json_generico y _sel_imagen_desde_abm (que siguen en cabledoc.py) resueltas con import diferido dentro de cada método que las usa; ídem _DialogoAltaRapidaCatalogo (que queda en catalogo_equipos_alta_rapida_ui.py, ver parte 2/2), mismo patrón de import diferido cruzado entre los dos módulos nuevos que ya se usó en la Entrega 3 entre equipos_ui.py/equipos_alta_rapida_ui.py. Import de nivel de módulo agregado: from pantallas_avanzadas import (abrir_coords_imagen, abrir_reglas_logicas_molde, abrir_editor_masivo_conectores_catalogo).
2026-09-04T01:15 catalogo_equipos_alta_rapida_ui.py: creado (Entrega 5 de plan_refactor_cabledoc.md, parte 2/2). Movida 1:1 desde cabledoc.py: _DialogoAltaRapidaCatalogo (líneas 2616-3106 antes de esta entrega, 491 líneas). Separada de catalogo_equipos_ui.py porque juntas superaban las ~900 líneas objetivo del plan (~1.200 → 2 archivos), mismo criterio de tamaño usado en la Entrega 3. Referencias cruzadas a MarcasListado y TiposConectorListado (que quedan en cabledoc.py) resueltas con import diferido; ídem _DialogoDireccionConector (que vive en equipos_ui.py desde la Entrega 3) y _DialogoDuplicarMolde (que queda en catalogo_equipos_ui.py, ver parte 1/2).
2026-09-04T01:15 cabledoc.py: el bloque "Catálogo de equipos (moldes)" (8 nombres: CatalogoEquiposListado, _DialogoConflictosImportacion, _DialogoCatalogoEquipo, _ConectoresCatalogoListado, _DialogoConectorCatalogo, _DialogoInstanciarCatalogo, _DialogoDuplicarMolde, _DialogoAltaRapidaCatalogo) reemplazado por dos `from ... import (...)` con la lista explícita de nombres movidos, agregados junto al resto de imports diferidos de nivel de módulo (mismo patrón que las Entregas 1-4). Cero cambios de comportamiento: todo el código de cabledoc.py que ya usaba estos 8 nombres sigue funcionando idéntico, mismo objeto reimportado. 6.167 → 4.986 líneas. APP_VERSION → 1.20260904011500.
2026-09-04T01:15 Validación de la Entrega 5 (checklist plan_refactor_cabledoc.md §5): (1) ast.parse + py_compile sobre cabledoc.py, catalogo_equipos_ui.py y catalogo_equipos_alta_rapida_ui.py: OK; (2) pyflakes comparado contra baseline de main (git show main:cabledoc.py): únicas advertencias nuevas son "imported but unused" del patrón de reexport esperado (los 8 nombres movidos + abrir_reglas_logicas_molde/abrir_editor_masivo_conectores_catalogo/_DialogoDireccionConector, que dejan de usarse directamente en cabledoc.py porque su único uso ahora vive en los módulos nuevos), cero F821 nuevos; (3) instalados gir1.2-gtk-3.0, python3-gi-cairo y pip graphqlite en el sandbox (no persisten entre sesiones); import real bajo Xvfb de cabledoc.py, catalogo_equipos_ui.py, catalogo_equipos_alta_rapida_ui.py, equipos_ui.py, cables_conexiones_ui.py, conectores_ui.py y pantallas_avanzadas.py: OK sin excepciones; identidad de objeto confirmada para los 8 nombres reexportados (cabledoc.X is catalogo_equipos_ui.X / catalogo_equipos_alta_rapida_ui.X) y para la referencia cruzada equipos_ui→cabledoc→CatalogoEquiposListado/_DialogoInstanciarCatalogo ya usada desde la Entrega 3; (4) smoke test funcional bajo Xvfb: a diferencia de las Entregas 2-4, el repo en esta sesión no traía database/db.db versionado (ni siquiera vacío) — se generó una base de fixture ejecutando schema_db.sql completo por sqlite3 directo, más 1 tipo_equipo/1 marca/1 tipo_conector/1 equipo_catalogo ("Molde Fixture")/2 conector_catalogo insertados por SQL directo. CatalogoEquiposListado cargó la fila del molde, _DialogoCatalogoEquipo abrió en modo edición (e_nombre.get_text()=="Molde Fixture") y en modo alta, _DialogoAltaRapidaCatalogo abrió sin excepciones, _ConectoresCatalogoListado cargó los 2 conectores-molde de fixture. Pendiente arrastrada (acumulada desde la Entrega 1): correr el smoke test contra el database/db.db real de Papi.
2026-09-03T22:38 equipos_ui.py: creado (Entrega 3 de plan_refactor_cabledoc.md, parte 1/2). Movidos 1:1 desde cabledoc.py: EquiposListado (líneas 2915-3143 antes de esta entrega), _DialogoDireccionConector + _DialogoEquipo (líneas 4263-5155). Los dos bloques no eran contiguos en el original —entre ellos quedan en cabledoc.py _DialogoDuplicarMolde y _DialogoAltaRapidaCatalogo, que corresponden al dominio de catálogo de equipos y se extraerán recién en la Entrega 5— así que se unieron en un único archivo de 1.207 líneas. Referencias cruzadas a CatalogoEquiposListado, _DialogoInstanciarCatalogo, ImagenesListado, MarcasListado, TiposEquipoListado, ConectoresListado, _DialogoRenombrarConectores y ProblemasEquipoListado (que siguen en cabledoc.py) resueltas con import diferido dentro de cada método que las usa. Import de nivel de módulo agregado: from pantallas_avanzadas import (abrir_historial_diagnosticos, abrir_arbol_conexiones, abrir_coords_imagen, abrir_diagrama_conexiones, abrir_editor_masivo_conectores, abrir_imagen_conectores, abrir_patcheras, abrir_reglas_logicas, abrir_vista_rack).
2026-09-03T22:38 equipos_alta_rapida_ui.py: creado (Entrega 3 de plan_refactor_cabledoc.md, parte 2/2). Movida 1:1 desde cabledoc.py: _DialogoAltaRapidaEquipo (líneas 3146-3685 antes de esta entrega, 540 líneas). Separada de equipos_ui.py porque juntas superaban las ~900 líneas objetivo del plan (~1.700 → 2 archivos, según diseño ya definido en plan_refactor_cabledoc.md §4). Referencia cruzada a _DialogoDireccionConector (que queda en equipos_ui.py) resuelta con import diferido; ídem MarcasListado y TiposConectorListado (que quedan en cabledoc.py).
2026-09-03T22:38 cabledoc.py: los 3 bloques (EquiposListado, _DialogoDireccionConector+_DialogoEquipo, _DialogoAltaRapidaEquipo) reemplazados por dos `from ... import (...)` con la lista explícita de los 4 nombres movidos, agregados junto al resto de imports diferidos de nivel de módulo (mismo patrón que las Entregas 1-2). Cero cambios de comportamiento: todo el código que ya usaba estos 4 nombres sigue funcionando idéntico, mismo objeto reimportado. 8.217 → 6.568 líneas (baja más de lo esperado por la sección: _DialogoDuplicarMolde/_DialogoAltaRapidaCatalogo, ~640 líneas, quedan pendientes para la Entrega 5). APP_VERSION → 1.20260903223802.
2026-09-03T22:38 Validación de la Entrega 3 (checklist plan_refactor_cabledoc.md §5): (1) ast.parse + py_compile sobre cabledoc.py, equipos_ui.py y equipos_alta_rapida_ui.py: OK; (2) import real de cabledoc.py bajo xvfb-run: OK, confirmados presentes los 4 nombres movidos + CatalogoEquiposListado/ImagenesListado/MarcasListado/TiposEquipoListado/ConectoresListado/_DialogoRenombrarConectores/ProblemasEquipoListado/TiposConectorListado; identidad de objeto confirmada (cabledoc.EquiposListado is equipos_ui.EquiposListado, cabledoc._DialogoDireccionConector is equipos_ui._DialogoDireccionConector, cabledoc._DialogoEquipo is equipos_ui._DialogoEquipo, cabledoc._DialogoAltaRapidaEquipo is equipos_alta_rapida_ui._DialogoAltaRapidaEquipo) y del import diferido cruzado entre los dos módulos nuevos (equipos_ui._DialogoDireccionConector is el mismo objeto que usará equipos_alta_rapida_ui.py); import limpio de los 4 archivos que dependen de símbolos de cabledoc (bitacora_ui.py, pantallas_avanzadas.py, diagrama_personalizado.py, cypher_console.py); (3) smoke test funcional bajo Xvfb contra una copia descartable de database/db.db (nunca la base real): EquiposListado instanciado en base vacía sin excepciones; insertada 1 fila de fixture por SQL directo (TEST-EQUIPO-1) para probar _DialogoEquipo en modo edición (id_equipo=1, verificado e_nombre.get_text()=="TEST-EQUIPO-1"); _DialogoEquipo en modo alta, _DialogoDireccionConector y _DialogoAltaRapidaEquipo abiertos sin excepciones. Pendiente arrastrada (no bloqueante, igual que en Entregas 1-2): repetir el smoke test contra la base real de Papi.
2026-09-03T22:04 cables_conexiones_ui.py: creado (Entrega 2 de plan_refactor_cabledoc.md). Movidas 1:1 desde cabledoc.py: CablesListado, _DialogoFusion, _DialogoEligeLadoFantasma, _DialogoCable, ConexionesListado, _DialogoConexion (860 líneas del bloque original "Cables y conexiones", líneas 5159-6018 de cabledoc.py antes de esta entrega). Referencias cruzadas a EquiposListado, _DialogoEquipo y _DialogoConector (que siguen viviendo en cabledoc.py) resueltas con import diferido dentro de los 2 métodos que las usan (_sel_equipo de _DialogoConexion, y el flujo de alta rápida de equipo+conector de _DialogoCable), mismo patrón de imports diferidos que ya usa el proyecto para evitar ciclos. Import de nivel de módulo agregado: from pantallas_avanzadas import abrir_historial_diagnosticos.
2026-09-03T22:04 pantallas_comunes.py: agregadas _parse_float_opt y _fmt_float_opt (movidas desde cabledoc.py, líneas 321-337 previas a esta entrega) — desviación deliberada respecto de plan_refactor_cabledoc.md, que no las mencionaba explícitamente: estaban duplicadas en uso entre el bloque de Cables (ancho de banda override, extraído a cables_conexiones_ui.py en esta entrega) y otros bloques que quedan en cabledoc.py (TipoCable, Problema), así que se consolidaron como helper genérico de formulario en vez de duplicarlas en dos módulos.
2026-09-03T22:04 cabledoc.py: bloque "Cables y conexiones" (CablesListado, _DialogoFusion, _DialogoEligeLadoFantasma, _DialogoCable, ConexionesListado, _DialogoConexion) reemplazado por `from cables_conexiones_ui import (...)` con la lista explícita de los 6 nombres, agregado junto al resto de imports diferidos de nivel de módulo (mismo patrón que la Entrega 1). Definiciones de _parse_float_opt/_fmt_float_opt removidas (movidas a pantallas_comunes.py, ver entrada de pantallas_comunes.py) y agregadas a la lista de reexport ya existente `from pantallas_comunes import (...)`. Cero cambios de comportamiento: todo el código de cabledoc.py que ya usaba estos 8 nombres sigue funcionando idéntico, mismo objeto reimportado. 9.079 → 8.217 líneas. APP_VERSION → 1.20260903220447.
2026-09-03T22:04 Validación de la Entrega 2 (checklist plan_refactor_cabledoc.md §5): (1) ast.parse + py_compile sobre cabledoc.py, cables_conexiones_ui.py y pantallas_comunes.py: OK; (2) instalado gir1.2-gtk-3.0, python3-gi-cairo y pip graphqlite en el sandbox (no estaban al inicio de la sesión); import real de cabledoc.py bajo xvfb-run: OK, confirmados presentes los 6 nombres movidos + EquiposListado/RacksListado/_DialogoEquipo/mostrar_error/etc.; identidad de objeto confirmada (cabledoc.CablesListado is cables_conexiones_ui.CablesListado, ídem los otros 5); import limpio de los 4 archivos que dependen de símbolos de cabledoc (bitacora_ui.py, pantallas_avanzadas.py, diagrama_personalizado.py, cypher_console.py); (3) smoke test funcional bajo Xvfb contra una copia descartable de database/db.db (nunca la base real): CablesListado y ConexionesListado instanciados en base vacía sin excepciones; insertados 2 cables de fixture directo por SQL (TEST-001, TEST-002) para probar _DialogoCable en modo edición (id_cable=1, verificado e_codigo.get_text()=="TEST-001") y _DialogoFusion con un par real de cables — ambos OK. Pendiente arrastrada (no bloqueante): repetir el smoke test contra la base real de Papi en vez de la sintética.
2026-09-03 00:00 - Fix: imágenes de referencia .svg (planos, layouts de conectores) se mostraban como "Sin imagen" en negro. Causas cubiertas: SVG sin width/height absolutos (fallback a viewBox), binding de Rsvg no instalado (ahora avisa en vez de fallar en silencio), y el "Explorar" del ABM Imagen que no sobrescribía un archivo preexistente con el mismo nombre. El visor de imágenes ahora usa fondo blanco (antes gris oscuro, se veía mal con SVGs de fondo transparente) y el placeholder "Sin imagen" muestra el motivo específico del fallo en vez de un mensaje genérico. Ver README.md (sección Requirements) para el nuevo requisito de sistema gir1.2-rsvg-2.0.
2026-09-03T02:09 Entrega 1 de plan_refactor_cabledoc.md (refactor de cabledoc.py, 9.405 líneas al inicio de esta entrega): consolidación de los helpers genéricos/base y de formulario de cabledoc.py en pantallas_comunes.py, en vez de crear un cabledoc_comunes.py nuevo — paso previo obligatorio del plan (§2): grep confirmó que pantallas_comunes.py ya tenía s() y confirmar() byte-idénticos a los de cabledoc.py (duplicados desde la Entrega 0 del refactor de pantallas_avanzadas.py), así que se agregaron ahí los 14 nombres restantes sin tocar los 2 ya existentes: mostrar_error, mostrar_info, _sort_func_natural, VentanaListado, DialogoNombre (sección "Clase base para ventanas de listado", movida 1:1 justo después de _ImagenZoom) y los 9 helpers de formulario _grid/_lbl_entry/_entry/_entry_btn/_searchable_combo/_get_combo_id/_set_combo_id/_repopulate_combo/_pack_ultima_edicion (sección nueva al final del archivo). pantallas_comunes.py: import from modelo ampliado a (Modelo, IMG_DIR) — antes solo IMG_DIR — porque _pack_ultima_edicion necesita Modelo.devolver_fecha_ultima_edicion(). 430 → 819 líneas (baseline previo al fix de SVG de main, que también tocó este archivo; ver diff real del PR para las líneas exactas post-merge).
2026-09-03T02:09 cabledoc.py: las 16 definiciones movidas (s, mostrar_error, mostrar_info, confirmar, _sort_func_natural, VentanaListado, DialogoNombre, _grid, _lbl_entry, _entry, _entry_btn, _searchable_combo, _get_combo_id, _set_combo_id, _repopulate_combo, _pack_ultima_edicion) reemplazadas por un único `from pantallas_comunes import (...)` con la lista explícita, agregado junto al resto de imports diferidos de nivel de módulo — mismo patrón ya usado para los 7 nombres que se importan de pantallas_avanzadas. Cero cambios en ningún otro punto del archivo: todo el código de cabledoc.py que ya usaba estos 16 nombres (miles de referencias) sigue funcionando idéntico porque son el mismo objeto reimportado, no una copia. APP_VERSION → 1.20260903020900.
2026-09-03T02:09 Validación de la Entrega 1 (checklist del plan §5 + pyflakes obligatorio desde el bug de la Entrega 2 del refactor de pantallas_avanzadas.py): (1) ast.parse sobre cabledoc.py y pantallas_comunes.py: OK; (2) pyflakes sobre ambos archivos comparado línea por línea contra el baseline (git stash): única advertencia nueva es "pantallas_comunes._sort_func_natural imported but unused" en cabledoc.py — reexport intencional (mismo patrón no-F821 que ya generaban los 7 imports de pantallas_avanzadas), el resto de los warnings (sys sin usar, f-strings sin placeholder, redefinición de DB_PATH, etc.) son 100% preexistentes, solo desplazados de línea; cero F821 (nombres indefinidos); (3) instalado gir1.2-gtk-3.0 + python3-gi-cairo + pip graphqlite en el sandbox de validación (no estaban al inicio de la sesión) y corrido bajo xvfb-run: import completo de cabledoc.py sin excepciones, confirmados presentes los 17 nombres de la tabla del plan §2 (incluida _DialogoEquipoNoRackSala), y verificada identidad de objeto (cabledoc.VentanaListado is pantallas_comunes.VentanaListado, ídem s/confirmar/DialogoNombre) — TRUE en los 4 casos, confirma reexport real, no duplicación; (4) smoke test funcional contra una base SQLite real generada desde schema_db.sql (modelo.asegurar_base_datos(), no la base de producción de Papi): instanciada MarcasListado (subclase real de VentanaListado, 0 filas en base vacía, sin excepciones) y DialogoNombre (con valor "Sony", .valor devuelve el texto esperado) bajo Xvfb. Validación rehecha después de rebasear sobre el fix de SVG de main (2026-09-03 00:00): sin conflicto de lógica, solo de líneas adyacentes en pantallas_comunes.py (el fix tocó _pixbuf_from_name/_ImagenZoom antes del punto de inserción de VentanaListado) y de APP_VERSION en cabledoc.py, resueltos manualmente. Sigue pendiente el smoke test visual contra database/db.db real de Papi, recomendado como primer paso antes de mergear el PR.
2026-09-04T03:30 senal_catalogo_ui.py: entrada retroactiva de changelog — este módulo (Entrega 6 de plan_refactor_cabledoc.md, 915 líneas: _DialogoSenal, SenalesListado, TiposFormatoSenalListado, _mostrar_donde_esta_senal, abrir_buscador_senal, _DialogoLinajeSenal, _ArbolLinajeSenal, _mostrar_lista_simple, _DialogoPropagacionSenal, abrir_propagacion_senal, _DialogoReportesSenal, abrir_reportes_senal, abrir_limpiar_senales_propagadas) ya estaba entregado y aplicado en el checkout de Papi (cabledoc.py ya reexportaba los 13 nombres, 4.986 → 4.150 líneas), pero la entrega quedó sin su entrada de changelog/PROGRESS por un corte de sesión. Esta entrada documenta retroactivamente el estado real de los archivos, confirmado por inspección directa del checkout antes de arrancar la Entrega 7 (ver PROGRESS_REFACTOR_CABLEDOC.md).
2026-09-04T05:00 racks_salas_ui.py: creado (Entrega 7 de plan_refactor_cabledoc.md, parte 1/2). Movidas 1:1 desde cabledoc.py: RacksListado, _DialogoRack, PosicionEnRackListado, _DialogoPosicionRack (bloque "Racks" contiguo, líneas 1091-1338 antes de esta entrega) + SalasListado, _DialogoRackPorSala, RackPorSalaListado, _DialogoEquipoNoRackSala, EquiposNoRackSalaListado (bloque "Salas" contiguo, líneas 2214-2431 antes de esta entrega). Los dos bloques no eran contiguos entre sí en el original —entre ellos quedan Frames/Slots (ver frames_slots_ui.py, parte 2/2) y ConexionesDeEquipoVentana (que permanece en cabledoc.py, fuera del alcance de esta entrega)— así que se unieron en un único archivo de 529 líneas, mismo criterio de tamaño usado en las Entregas 3 y 5. Referencia cruzada a EquiposListado (que sigue en equipos_ui.py, reexportado desde cabledoc.py) y a FramesListado (que se extrae en el archivo hermano de esta misma entrega, frames_slots_ui.py) resueltas con import diferido dentro de los métodos que las usan, siempre ruteando a través de `from cabledoc import X` — mismo patrón ya usado por cables_conexiones_ui.py con EquiposListado, incluso para referencias entre los dos módulos hermanos de esta entrega. Import de nivel de módulo agregado: from pantallas_avanzadas import abrir_vista_rack; from modelo import Modelo, DB_PATH (para el botón "Simular remoción" de _DialogoRack, que también mantiene su import diferido preexistente de impacto_ui.simular_remocion_y_mostrar sin cambios).
2026-09-04T05:00 frames_slots_ui.py: creado (Entrega 7 de plan_refactor_cabledoc.md, parte 2/2). Movido 1:1 desde cabledoc.py el bloque contiguo completo "Catálogo de frames" + "Frames" + "Slots" (líneas 1340-2147 antes de esta entrega, 805 líneas): CatalogoFramesListado, _DialogoCatalogoFrame, _SlotsCatalogoListado, _DialogoSlotCatalogo, _DialogoInstanciarCatalogoFrame, FramesListado, _DialogoFrame, SlotsListado, _DialogoSlot. A diferencia de la Entrega 3, este fue un bloque contiguo sin sorpresas de layout. Referencias cruzadas a MarcasListado, ImagenesListado, EquiposListado (que siguen en cabledoc.py/equipos_ui.py) y a _escribir_json_comprimido, _leer_json_generico, _sel_imagen_desde_abm (helpers module-level que permanecen en cabledoc.py) resueltas con import diferido dentro de cada método que las usa — mismo patrón exacto ya usado en catalogo_equipos_ui.py (Entrega 5) para estos tres últimos helpers. Import de nivel de módulo agregado: from pantallas_avanzadas import (abrir_coords_imagen, abrir_vista_rack, abrir_vista_frame_slots, abrir_editor_masivo_slots, abrir_editor_masivo_slots_catalogo).
2026-09-04T05:00 cabledoc.py: los 3 bloques (Racks+Posición en Rack, Catálogo de frames+Frames+Slots, Salas+Rack por Sala+Equipo no rackeado) reemplazados por dos `from ... import (...)` con la lista explícita de los 18 nombres movidos (9 + 9), agregados junto al resto de imports diferidos de nivel de módulo, inmediatamente después del bloque de reexport de senal_catalogo_ui.py (Entrega 6). ConexionesDeEquipoVentana permanece sin cambios en cabledoc.py (no forma parte de ningún dominio de esta entrega, según plan_refactor_cabledoc.md §3/§4). Cero cambios de comportamiento: todo el código que ya usaba estos 18 nombres (incluidos rack_ui.py vía RacksListado/_DialogoFrame) sigue funcionando idéntico, mismo objeto reimportado. 4.150 → 2.917 líneas. APP_VERSION → 1.20260904050000.
2026-09-04T05:00 Validación de la Entrega 7 (checklist plan_refactor_cabledoc.md §5): (1) ast.parse + py_compile sobre cabledoc.py, racks_salas_ui.py y frames_slots_ui.py: OK; (2) diff línea por línea de cada bloque movido contra su ubicación original en cabledoc.py (script propio de comparación, excluyendo únicamente las líneas `from cabledoc import ...` agregadas y el diferencial de líneas en blanco en la costura artificial entre los dos sub-bloques que comparten archivo): idéntico carácter por carácter, cero cambios de lógica; (3) diff completo de cabledoc.py contra el original: confirma que solo se tocaron 4 zonas (APP_VERSION, bloque de reexport nuevo, y los dos bloques removidos) — nada más; (4) grep de "from cabledoc import" en los 12 archivos externos que dependen de símbolos de cabledoc.py (equipos_ui.py, rack_ui.py, patcheras_ui.py, cables_conexiones_ui.py, catalogo_equipos_ui.py, catalogo_equipos_alta_rapida_ui.py, equipos_alta_rapida_ui.py, conectores_ui.py, bitacora_ui.py, diagrama_personalizado.py, arbol_conexiones_ui.py, grafo_diagrama_ui.py, frame_slots_ui.py, interaccion_diagrama_ui.py, extension_cable_ui.py, cypher_console.py) confirma que RacksListado y _DialogoFrame (los dos nombres de esta entrega referenciados externamente, ambos desde rack_ui.py) siguen resolviendo sin cambios; (5) chequeo manual de nombres libres por función (script ast propio, sustituto de pyflakes) sobre los dos archivos nuevos: sin hallazgos reales (único falso positivo esperado es `_` del bootstrap de i18n dentro de try/except, que el script no captura). **Pendiente, no bloqueante:** este sandbox no tuvo acceso de red en esta sesión, así que no se pudo instalar gir1.2-gtk-3.0/python3-gi-cairo ni graphqlite — no se corrió pyflakes real ni import bajo Xvfb esta vez (a diferencia de las Entregas 1-6). Recomendado que Fede corra el checklist completo de validación (import real + Xvfb + smoke test funcional) en su entorno antes de mergear, igual que arrastra el resto de las entregas contra database/db.db real.
2026-09-04T06:00 catalogos_basicos_ui.py: creado (Entrega 8 de plan_refactor_cabledoc.md). Movidas 1:1 desde cabledoc.py: MarcasListado, _DialogoTipoEquipo, TiposEquipoListado, TiposConectorListado, RiesgoSenalListado, _DialogoTipoCable, TiposCableListado (bloque contiguo "catálogos chicos", líneas 244-1031 antes de esta entrega) + _DialogoTipoFicha, TiposFichaListado, CategoriasProblemaListado, ProblemasEquipoListado, _DialogoProblema, ImagenesListado, _DialogoImagen (mismo bloque contiguo, continúa hasta línea 1031) + ConexionesDeEquipoVentana, DiagramasGuardadosListado, GeneradorDiagrama, EquipoInfoExtra (bloque contiguo "huérfanos", líneas 1135-1350 antes de esta entrega, separado del anterior por _DialogoRenombrarConectoresCatalogo que queda en cabledoc.py, ver más abajo). Desviación deliberada respecto de plan_refactor_cabledoc.md §4: el plan solo asignaba a este módulo los "catálogos chicos" (~774 líneas estimadas); ConexionesDeEquipoVentana, DiagramasGuardadosListado y GeneradorDiagrama nunca tuvieron módulo destino asignado en el plan (huérfanos ya en el relevamiento original de §3) y EquipoInfoExtra ni siquiera existía cuando se escribió el plan. Como la Entrega 9 (panel_arbol_ui.py) es la última antes del cierre de la Entrega 10 (cabledoc.py a ~700 líneas), la Entrega 8 es el único punto que queda con perfil de "catch-all" para estos 4 bloques — se documentó la desviación en el docstring del módulo nuevo en vez de dejarlos varados. Resultado: 1.007 líneas de código movido (antes de header/imports) en vez de las ~774 estimadas. Referencia cruzada a _DialogoCable (que vive en cables_conexiones_ui.py desde la Entrega 2, usado en RiesgoSenalListado.editar) resuelta con import diferido dentro del método, ruteando a través de `from cabledoc import _DialogoCable` — mismo patrón ya establecido por racks_salas_ui.py con FramesListado. Import de nivel de módulo agregado: from modelo import Modelo, DB_PATH, IMG_DIR; from diagrama_personalizado import DiagramaPersonalizado; from pantallas_comunes import (s, mostrar_error, VentanaListado, DialogoNombre, _grid, _lbl_entry, _entry, _get_combo_id, _set_combo_id, _repopulate_combo, _searchable_combo, _parse_float_opt, _fmt_float_opt, _pack_ultima_edicion).
2026-09-04T06:00 cabledoc.py: el bloque "catálogos chicos" + el bloque "huérfanos" (18 nombres en total) reemplazados por `from catalogos_basicos_ui import (...)` con la lista explícita de los nombres movidos, agregado inmediatamente después del bloque de reexport de frames_slots_ui.py (Entrega 7). _DialogoRenombrarConectoresCatalogo permanece sin cambios en cabledoc.py, con un comentario de sección propio explicando que queda pendiente de asignar destino en una entrega futura (no forma parte de ningún dominio de la Entrega 8) — sigue siendo usado desde catalogo_equipos_ui.py vía import diferido preexistente, sin cambios. Cero cambios de comportamiento: todo el código que ya usaba los 18 nombres movidos (incluida toda la construcción de menús de VentanaPrincipal) sigue funcionando idéntico, mismo objeto reimportado. 2.917 → 1.949 líneas. APP_VERSION → 1.20260904060000.
2026-09-04T06:00 Validación de la Entrega 8 (checklist plan_refactor_cabledoc.md §5): (1) ast.parse + py_compile sobre cabledoc.py y catalogos_basicos_ui.py: OK; (2) diff línea por línea de cada bloque movido contra su ubicación original en el checkout de main (git show main:cabledoc.py): idéntico carácter por carácter salvo el único import diferido agregado (from cabledoc import _DialogoCable) — confirma además que _DialogoRenombrarConectoresCatalogo, intencionalmente excluido del *move*, no aparece en el archivo nuevo; (3) pyflakes sobre ambos archivos comparado línea por línea contra baseline de main: catalogos_basicos_ui.py sin ninguna advertencia; en cabledoc.py, únicas advertencias nuevas son "imported but unused" del patrón de reexport esperado (los 18 nombres movidos + subprocess/IMG_DIR/DiagramaPersonalizado/mostrar_error/VentanaListado/_grid/_lbl_entry/_entry/_searchable_combo/_get_combo_id/_set_combo_id/_repopulate_combo/_pack_ultima_edicion/_parse_float_opt/_fmt_float_opt, que dejaban de usarse directamente en cabledoc.py porque su único uso ahora vive en el módulo nuevo), cero F821 nuevos, el resto de warnings preexistentes solo desplazados de línea; (4) instalados gir1.2-gtk-3.0, python3-gi-cairo, xvfb y pip graphqlite en el sandbox (no persisten entre sesiones); import real bajo Xvfb de cabledoc.py y catalogos_basicos_ui.py: OK sin excepciones, identidad de objeto confirmada para los 18 nombres reexportados (cabledoc.X is catalogos_basicos_ui.X) y para _DialogoRenombrarConectoresCatalogo (cabledoc.X, no movido); import limpio de los 13 módulos que dependen de símbolos de cabledoc (equipos_ui.py, equipos_alta_rapida_ui.py, catalogo_equipos_ui.py, catalogo_equipos_alta_rapida_ui.py, conectores_ui.py, frames_slots_ui.py, racks_salas_ui.py, senal_catalogo_ui.py, cables_conexiones_ui.py, pantallas_avanzadas.py, bitacora_ui.py, diagrama_personalizado.py, cypher_console.py); (5) smoke test funcional bajo Xvfb contra una copia descartable de database/db.db (a diferencia de las Entregas 3-5, el repo en esta sesión sí traía database/db.db versionado, con esquema pero sin datos) con 1 fila de fixture por tabla (marca/tipo_equipo/tipo_conector/tipo_cable/imagen/categoria_problema/equipo) insertada por SQL directo: MarcasListado, TiposEquipoListado, TiposConectorListado, TiposCableListado, ImagenesListado, CategoriasProblemaListado, ProblemasEquipoListado, DiagramasGuardadosListado y RiesgoSenalListado instanciados sin excepciones; _DialogoImagen y _DialogoProblema abiertos en modo alta; GeneradorDiagrama.generar_dot_equipo() y ConexionesDeEquipoVentana instanciados sin excepciones contra el equipo de fixture. **No se pudo instanciar EquipoInfoExtra en este smoke test**: su __init__ llama a ConexionesDeEquipoVentana(...).run(), un diálogo modal que bloquea el loop de GTK esperando una respuesta interactiva — no hay forma de simularla desde un script sin interfaz. Se cubrió por otra vía: diff byte a byte confirmado idéntico al original (ver punto 2) y sus dos dependencias directas (ConexionesDeEquipoVentana, GeneradorDiagrama) validadas por separado sin excepciones. Nota aparte, no introducida por esta entrega: el código de EquipoInfoExtra instancia ConexionesDeEquipoVentana dos veces (una para .run(), otra asignada a conexiones_widget con el comentario "In practice just show the dialog separately") — parece código muerto/incompleto preexistente, no tocado por ser un *move* puro sin cambios de lógica. Pendiente arrastrada (acumulada desde la Entrega 1): correr el smoke test contra el database/db.db real de Papi, incluyendo idealmente una prueba manual interactiva de EquipoInfoExtra.
2026-09-04T23:00 panel_arbol_ui.py: creado (Entrega 9 de plan_refactor_cabledoc.md). Movida 1:1 desde cabledoc.py: PanelArbol (877 líneas, líneas 390-1266 antes de esta entrega). Última entrega antes del cierre (Entrega 10) a propósito, según el plan: PanelArbol es el orquestador visual que referencia diálogos de todos los dominios ya extraídos en las Entregas 1-8 (equipos, cables, conectores, racks, frames), así que sus referencias cruzadas ya apuntan a los módulos finales y no hace falta retocarlas de nuevo en una entrega posterior. Confirmado por grep que PanelArbol no tiene ningún consumidor externo fuera de cabledoc.py (a diferencia de RacksListado/_DialogoFrame en la Entrega 7 o MarcasListado/ImagenesListado en la Entrega 5) — se reexporta igual desde cabledoc.py por consistencia con el resto de la fachada. Referencias cruzadas a _DialogoEquipo, _DialogoRack, _DialogoFrame, _DialogoConector, _DialogoCable, _DialogoConexion (usadas en el dispatcher _on_row_activated) y _DialogoPosicionRack (usada en _on_drag_data_received) resueltas con import diferido dentro de cada método, ruteando vía `from cabledoc import X` — mismo patrón ya establecido por racks_salas_ui.py con FramesListado y catalogos_basicos_ui.py con _DialogoCable. El import diferido preexistente de _DialogoEquipoNoRackSala (ya estaba dentro de PanelArbol desde antes de esta entrega, necesario en su momento para evitar ciclo cuando PanelArbol todavía vivía en cabledoc.py) se preservó sin cambios, sólo combinado en la misma línea con _DialogoPosicionRack porque ambos se usan en el mismo método (_on_drag_data_received). s() y DialogoNombre (genéricos de pantallas_comunes.py) se importan directo a nivel de módulo, igual que en racks_salas_ui.py. Import de nivel de módulo agregado: from gi.repository import Gtk, Gdk, GLib, GObject; import time; from datetime import datetime; from modelo import Modelo; from pantallas_comunes import s, DialogoNombre.
2026-09-04T23:00 cabledoc.py: el bloque PanelArbol reemplazado por `from panel_arbol_ui import PanelArbol`, agregado inmediatamente después del bloque de reexport de catalogos_basicos_ui.py (Entrega 8) y antes de VentanaPrincipal. _DialogoRenombrarConectoresCatalogo permanece sin cambios (no forma parte de la Entrega 9, sigue pendiente de asignar destino, ver nota de la Entrega 8). Cero cambios de comportamiento: VentanaPrincipal sigue instanciando PanelArbol(self) igual que antes, mismo objeto reimportado. 1.949 → 1.080 líneas. APP_VERSION → 1.20260904230000.
2026-09-04T23:00 Validación de la Entrega 9 (checklist plan_refactor_cabledoc.md §5): (1) ast.parse + py_compile sobre cabledoc.py y panel_arbol_ui.py: OK; (2) diff línea por línea del bloque movido contra su ubicación original en el checkout de main (git show main:cabledoc.py): idéntico carácter por carácter salvo los dos imports diferidos agregados/combinados (el de _on_row_activated, nuevo, y el de _on_drag_data_received, combinado con _DialogoPosicionRack); diff completo de cabledoc.py contra main confirma que solo se tocó una zona contigua (bloque PanelArbol reemplazado por el reexport) más APP_VERSION; (3) grep de "PanelArbol" en todo el repo confirma que su único uso fuera de panel_arbol_ui.py/cabledoc.py es la instanciación PanelArbol(self) dentro de VentanaPrincipal — no hay ningún archivo externo que dependa de este nombre; (4) pyflakes sobre ambos archivos comparado línea por línea contra baseline de main: única categoría de advertencias nuevas en cabledoc.py es "imported but unused" del patrón de reexport esperado (Gdk/GLib/GObject/time/datetime.datetime + los 7 diálogos de dominio que sólo se usaban dentro de PanelArbol y ahora sólo se usan vía import diferido en panel_arbol_ui.py), cero F821 nuevos; panel_arbol_ui.py replica exactamente las mismas advertencias preexistentes que ya tenía este código dentro de cabledoc.py (sys sin usar, variable local "e" sin usar, 3 f-strings sin placeholder), solo con números de línea desplazados; (5) instalados gir1.2-gtk-3.0, python3-gi-cairo, xvfb y pip graphqlite en el sandbox (esta sesión sí tuvo acceso de red); import real bajo Xvfb: OK, identidad de objeto confirmada (cabledoc.PanelArbol is panel_arbol_ui.PanelArbol) y también para el import diferido combinado (cabledoc._DialogoEquipoNoRackSala is racks_salas_ui._DialogoEquipoNoRackSala, cabledoc._DialogoPosicionRack is racks_salas_ui._DialogoPosicionRack); (6) smoke test funcional bajo Xvfb contra una copia descartable de database/db.db (el repo en esta sesión no traía database/db.db versionado, generada desde schema_db.sql, mismo patrón que las Entregas 2-5) con fixture de 1 sala + 1 rack (vinculado a la sala) + 1 equipo en rack + 1 equipo suelto + 2 conectores + 1 cable + 1 conexión insertados por SQL directo: PanelArbol instanciado sin excepciones, recargar(forzar_bd=True) construyó el árbol completo (11 filas, tipos sala/rack/equipo/conector/cable/conexion/sin_rack presentes), y _on_row_activated ejercitado con mocks para los 5 tipos de nodo con diálogo asociado (equipo/rack/conector/cable/conexion), confirmando en cada caso que el import diferido dentro del método resuelve al símbolo correcto. La rama de drag&drop (_on_drag_data_received) no se pudo ejercitar con un evento GTK real (requiere simular un drag interactivo, no factible desde un script headless) — se cubrió por vía alternativa: diff byte a byte confirmado (ver punto 2) más verificación de identidad de objeto directa del import diferido combinado. Pendiente arrastrada (acumulada desde la Entrega 1): correr el smoke test contra el database/db.db real de Papi, incluyendo idealmente una prueba manual interactiva de arrastrar un equipo suelto a un rack/sala.
2026-09-07T19:00 planos_ui.py (Fase 9 de plan_desarrollo_ubicacion_fisica_planos.md, "Pulido"): `VistaPlanoInteractivo` gana color propio para equipo suelto de Pared (`COLOR_SUELTO_PARED`, cian) además del ya existente de Piso (`COLOR_SUELTO`, violeta) — antes ambos compartían color y sólo se distinguían por borde sólido/punteado + ícono 🖴/🧱 (Fase 7); ahora las 5 categorías del overlay (sala/rack/mueble/suelto-piso/suelto-pared) tienen color propio, sin sacar la distinción de borde/ícono que sigue siendo la principal a bajo zoom. Tooltip nuevo al pasar el mouse sobre cualquier elemento del overlay (sala/rack/mueble/equipo suelto), vía `has-tooltip`/`query-tooltip` sobre `self._viz.da`: `_hit_test_overlay()` (nuevo, generaliza a los 4 tipos el hit-testing puntual que ya usaba el clic sobre un rack en Fase 7) prueba primero rack/mueble/equipo suelto (puntual/rectangular) y al final el contorno de sala (`_punto_en_poligono`, ray casting nuevo) para no taparle el hit-test a lo que tiene adentro; muestra "Tipo: Nombre" (ej. "Rack: Rack Audio"). Para no repetir `Modelo.devolver_contenido_plano()` (consulta agregada) en cada movimiento del mouse — awareness de N+1 del proyecto — se agregó `self._contenido_cache`, poblado en cada `_dibujar_overlay` y reusado tanto por el tooltip nuevo como por `_on_click_overlay` (Fase 7, que antes repetía la consulta en cada clic; ahora usa el cache con fallback a consulta fresca si todavía no se dibujó nada). Zoom-to-fit al abrir ya estaba resuelto desde Fase 4/5 (`_on_viz_realize` → `self._viz._zoom_fit()`), sin cambios. Con esta entrega se cierra `plan_desarrollo_ubicacion_fisica_planos.md` (Fases 1 a 9 completas).
2026-09-07T19:00 cabledoc.py: sólo APP_VERSION → 1.20260907190000 (sin cambios de reexport — planos_ui.py ya estaba completo en la fachada desde fases anteriores).
2026-09-07T19:00 Validación de la Fase 9 de plan_desarrollo_ubicacion_fisica_planos.md (mismo checklist que las fases anteriores del plan): (1) ast.parse + py_compile sobre planos_ui.py y cabledoc.py: OK; (2) pyflakes sobre planos_ui.py comparado contra el HEAD previo a este cambio (git stash): cero advertencias, en ambos casos (antes y después) — sin hallazgos nuevos; (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, python3-gi-cairo, gir1.2-rsvg-2.0, xvfb y pip pyflakes/graphqlite; (4) smoke test funcional bajo Xvfb contra una base SQLite generada desde schema_db.sql + Modelo.asegurar_tablas_plano() (corrida dos veces, idempotente) con 1 plano (imagen SVG de 1000x800)/1 sala con contorno de 4 vértices/1 rack ubicado/1 mueble ubicado/2 equipos sueltos (uno Piso, uno Pared, reubicado sin solape con el mueble para la prueba de color/ícono): `VistaPlanoInteractivo` instanciada, `_dibujar_overlay` corrido contra un `cairo.Context` real sin excepciones, cache `_contenido_cache` poblado; `_hit_test_overlay` confirmado correcto contra los 4 tipos (rack, mueble, equipo suelto Piso, equipo suelto Pared) y contra el interior del polígono de la sala, y `None` fuera de cualquier elemento; `_on_query_tooltip` ejercitado con un `Gtk.Tooltip` real y coordenadas de pantalla (convertidas ida y vuelta con `i2w`/`w2i`) — devuelve `True` con el texto "Rack: Rack Audio" sobre el punto del rack y `False` fuera de todo; regresión de `_on_click_overlay` (Fase 7) confirmada usando el mismo cache, sin repetir la consulta a `Modelo.devolver_contenido_plano`; colores `COLOR_SUELTO`/`COLOR_SUELTO_PARED` confirmados distintos. (5) smoke test de arranque adicional: `VentanaPrincipal` completa instanciada bajo Xvfb sin excepciones contra la misma base de fixture. Pendiente arrastrada (acumulada desde fases anteriores del plan): correr el smoke test visual completo contra el `database/db.db` real de Fede, en particular confirmar que los tooltips y los colores nuevos se ven bien sobre la imagen real de un plano con datos reales (nunca ejercitado con imágenes/datos de producción).
2026-09-10T04:15 catalogos_basicos_ui.py: Parte 2 de plan_referencia_virtual_frame.md (UI, sobre Parte 1 ya mergeada en main vía PR #25/commit 968fe77). Discrepancia detectada contra el plan antes de escribir código (confirmada clonando main): el plan §5 daba por existente "el checkbox que ya existe para es_referencia_generada" en el catálogo de tipo de conector, pero TiposConectorListado usaba DialogoNombre genérico (solo nombre) — direccion/es_referencia_generada solo eran editables vía el mecanismo de import/conflictos de catálogo (_DialogoConflictosImportacion en catalogo_equipos_ui.py). El propio comentario dejado en modelo.py por Parte 1 (asegurar_columnas_control_idioma, ~línea 6053) ya prometía "TiposConectorListado / _DialogoTipoConector" como vía de edición manual de es_entrada_referencia, pero esa clase no existía. Resuelto con el usuario: se crea _DialogoTipoConector (nombre + checkbox "Es referencia generada" + checkbox "Entrada de referencia (hereda del frame si no se cablea)"), reemplaza a DialogoNombre en TiposConectorListado.nuevo()/editar(). direccion NO se expone en este diálogo (fuera de lo pedido, sigue poblada por heurística de nombre + import). _DialogoTipoEquipo.ROLES gana la entrada DISTRIBUIDOR_FRAME (mismo patrón que PATCHERA/CONVERSOR_BALANCE/SUMADOR_CANAL; FANTASMA se mantiene fuera de este combo a propósito, es autoasignado). Decisión pendiente del plan (regla_logica REF1 IN → REFOUT interno automática vs. manual) se deja MANUAL por ahora, sin tocar — sigue documentada como pendiente en plan_referencia_virtual_frame.md.
2026-09-10T04:15 modelo.py: Modelo.agregar_tipo_conector(nombre) ahora devuelve el id_tipo_conector recién insertado (antes no devolvía nada) — necesario para que _DialogoTipoConector pueda aplicar los checkboxes de control de idioma inmediatamente después del alta en el mismo flujo, sin tener que buscar el id por nombre (tipo_conector.nombre no tiene UNIQUE). Cambio retrocompatible: no había otros llamadores que dependieran del valor de retorno anterior (None).
2026-09-10T04:15 graph_impact.py: etiquetado cosmético de conexiones virtuales de referencia (plan_referencia_virtual_frame.md, punto 5) — nueva función módulo `_etiqueta_nombre_cable(codigo)` que reemplaza el código de cable "REF-VIRTUAL-*" por "Referencia heredada del frame" en el único punto donde se arma el nombre de cable físico normal (_leer_bd, bloque de CONEXIONES_AMBOS_EXTREMOS); todo el resto de graph_impact.py sigue sin cambios estructurales, tal como especifica el plan (la conexión virtual se recorre exactamente igual que un cable real en el grafo, esto es puramente de presentación en impacto_ui.py/escenario_engine.py, que consumen nombre_cable() sin cambios). Prefijo _PREFIJO_CABLE_REF_VIRTUAL replicado localmente (no se importa Modelo en este módulo, mismo criterio defensivo ya usado para direccion/es_referencia_generada/rol_senal en este mismo archivo). No cubre el bloque de cables que atraviesan extension_cable (líneas ~1001-1030) porque una conexión virtual de referencia es un cable de 2 puntas directo, no pasa por extension_cable — fuera de alcance real, no una omisión.
2026-09-10T04:15 Validación de Parte 2 de plan_referencia_virtual_frame.md: (1) ast.parse + py_compile sobre catalogos_basicos_ui.py, modelo.py y graph_impact.py: OK; (2) pyflakes sobre los 3 archivos comparado contra baseline de main (git stash): cero advertencias nuevas en los 3; (3) sandbox con acceso de red: instalados gir1.2-gtk-3.0, python3-gi-cairo, xvfb y pip graphqlite; import real bajo Xvfb de catalogos_basicos_ui.py y cabledoc.py sin excepciones, identidad de objeto confirmada (cabledoc.TiposConectorListado is catalogos_basicos_ui.TiposConectorListado, cabledoc._DialogoTipoEquipo is catalogos_basicos_ui._DialogoTipoEquipo); _DialogoTipoConector y _DialogoTipoEquipo(rol_senal='DISTRIBUIDOR_FRAME') instanciados directamente bajo Xvfb sin excepciones, valores de checkbox/combo leídos correctamente; (4) smoke test funcional contra SQLite sintética generada desde schema_db.sql (no database/db.db real): a) alta de tipo_conector + ambos checkboxes + relectura vía devolver_control_idioma_tipo_conector — valores correctos; b) tipo_equipo con rol_senal=DISTRIBUIDOR_FRAME dado de alta y releído correctamente; c) TiposConectorListado y TiposEquipoListado instanciados contra la base de fixture sin excepciones (evitando deliberadamente dlg.run() de _DialogoTipoConector/_DialogoTipoEquipo en el smoke headless porque bloquea el loop modal esperando interacción — se validó la precarga de datos hacia el diálogo por separado, sin abrir el modal); d) flujo end-to-end completo de Parte 1 re-confirmado sin tocarlo: alta de frame + equipo DISTRIBUIDOR_FRAME con conector "REF OUT INTERNO" + módulo con conector es_entrada_referencia=1 sin cable propio → Modelo.agregar_slot() dispara sincronizar_referencia_virtual_frame() y crea el cable+2 conexiones REF-VIRTUAL-<frame>-<conector> automáticamente; e) graph_impact._etiqueta_nombre_cable() confirmado: devuelve "Referencia heredada del frame" para códigos REF-VIRTUAL-*, código sin cambios para cualquier otro. **Pendiente arrastrada (acumulada desde entregas anteriores):** correr el smoke test contra el database/db.db real de Papi; no se pudo ejercitar el flujo completo de impacto (GraphImpactAnalyzer.simular_desconexion) de punta a punta con REF-VIRTUAL en esta sesión por requerir además una regla_logica configurada en el equipo DISTRIBUIDOR_FRAME (decisión de dejarla manual, ver arriba) — recomendado que Fede la configure a mano en un caso de prueba real y corra un "Análisis de impacto" sobre el cable físico REF1/REF2 para confirmar visualmente el flujo completo, incluyendo la etiqueta cosmética nueva. APP_VERSION → 1.20260910041500.
2026-09-10T05:00 CORRECCIÓN sobre la entrega anterior (2026-09-10T04:15, misma sesión, todavía no aplicada/commiteada por Fede): Fede marcó con una captura de "Editar Conector" que es_entrada_referencia estaba mal ubicado en tipo_conector — un tipo de conector compartido (ej. "BNC") no garantiza nada sobre si UN conector puntual de un equipo real necesita heredar referencia del frame, puede estar reusado en equipos sin ninguna relación con referencia. Se corrige moviendo es_entrada_referencia de tipo_conector a conector (atributo de la instancia real, igual que fila_patchera/es_armado_correcto/id_tipo_ficha, que ya eran todos por-conector). direccion y es_referencia_generada NO se tocan — son Fase 7 de plan_desarrollo_hardcodes_idioma.md, preexistentes a este plan, con semántica de tipo_conector legítima y usada en otros lugares (detección de fuente SPG/wordclock en signal_risk.py, dirección IN/OUT del grafo) — sacarlas de ahí sería un cambio no pedido y de alcance mucho mayor.
2026-09-10T05:00 modelo.py: `asegurar_columnas_control_idioma()` ya NO agrega es_entrada_referencia a tipo_conector; la agrega a `conector` (bloque junto a fila_patchera), sin semilla por heurística de nombre (mismo criterio que la versión anterior: arranca en 0, se marca a mano). `establecer_es_entrada_referencia_tipo_conector` eliminada, reemplazada por `establecer_es_entrada_referencia_conector(id_conector, valor)` (dispara resync inmediato) + `devolver_es_entrada_referencia_conector(id_conector)`. `devolver_control_idioma_tipo_conector` vuelve a ser un 2-tuple (direccion, es_referencia_generada) — es_entrada_referencia ya no aplica a ese nivel. `sincronizar_referencia_virtual_frame()` y `_resync_referencia_virtual_si_aplica()` actualizadas para leer `conector.es_entrada_referencia` directamente (sin JOIN a tipo_conector).
2026-09-10T05:00 modelo.py: bug real encontrado durante el smoke test de esta corrección (no relacionado a dónde vive la columna, habría pasado igual si hubiéramos hecho el diseño correcto desde el principio): `sincronizar_referencia_virtual_frame()` sólo recorría los conectores que EN ESE MOMENTO tienen es_entrada_referencia=1 para decidir altas/bajas de la virtual — si un conector se destildaba (dejaba de calificar), su virtual vieja quedaba huérfana para siempre, porque el conector ya no aparecía en el scan. Se agregó un paso de limpieza que recorre todas las virtuales existentes de ese frame (el id del conector-entrada va codificado en el propio código de cable "REF-VIRTUAL-<frame>-<conector>", no hace falta JOIN extra) y borra las que ya no califican. Confirmado con smoke test: marcar → crea virtual, desmarcar → borra virtual, re-marcar → vuelve a crear.
2026-09-10T05:00 catalogos_basicos_ui.py: `_DialogoTipoConector` pierde el checkbox de `es_entrada_referencia` (quedó sólo el de `es_referencia_generada`, que sí es correcto a nivel tipo_conector — Fase 7 preexistente). `TiposConectorListado.nuevo()`/`.editar()` actualizados.
2026-09-10T05:00 conectores_ui.py: `_DialogoConector` (ficha real "Editar Conector" de un equipo, la que Fede muestra en la captura) gana una sección nueva "Referencia" con el checkbox "Entrada de referencia (hereda del frame si no se cablea)", mismo patrón visual/de guardado que las secciones Formato eléctrico/Armado ya existentes (Gtk.Frame + grid, sólo visible con el conector ya guardado, se aplica al Aceptar de la ficha completa). Precarga con `Modelo.devolver_es_entrada_referencia_conector()`, guarda con `Modelo.establecer_es_entrada_referencia_conector()` en `run_and_destroy()`.
2026-09-10T05:00 Validación de la corrección: (1) ast.parse + pyflakes sobre modelo.py, catalogos_basicos_ui.py, conectores_ui.py (comparado contra baseline de main): cero advertencias nuevas; (2) smoke test funcional contra SQLite sintética con un caso pensado específicamente para probar el fix: un mismo tipo_conector "BNC" usado en DOS equipos sin relación entre sí (un módulo con "REF IN BNC" real necesitando referencia, y una cámara con "VIDEO IN BNC" que no tiene nada que ver) — confirmado que marcar/desmarcar el checkbox en el conector puntual de un equipo NUNCA afecta al conector del otro equipo (antes de este fix, al estar a nivel tipo_conector, marcar UNO los hubiera afectado a los dos); marcar → crea la virtual; desmarcar → la borra (bug de limpieza incluido, ver arriba); re-marcar → la vuelve a crear; `devolver_es_entrada_referencia_conector()` devuelve el valor correcto por conector; (3) smoke test GTK bajo Xvfb: `_DialogoConector` real instanciado contra un conector con el flag ya tildado en la base, checkbox se precarga tildado correctamente; identidad de reexport de `cabledoc.py` confirmada para `ConectoresListado`/`TiposConectorListado`. **Sigue pendiente (sin cambios respecto a la entrega anterior):** correr contra `db.db` real; regla_logica REF1→REFOUT sigue manual. APP_VERSION → 1.20260910050000.
2026-09-10T05:30 SEGUNDA CORRECCIÓN sobre la entrega de esta misma sesión (todavía no aplicada/commiteada por Fede): consulta de Fede — ¿esta solución no rompe el trabajo de eliminación de hardcodes, dado que un usuario en español o inglés que cambie el valor de "REF OUT INTERNO" haría que se rompa todo en forma silenciosa? Confirmado: sí, era un bug real. `sincronizar_referencia_virtual_frame()` ubicaba el conector "fuente" del equipo DISTRIBUIDOR_FRAME comparando `UPPER(c.nombre)='REF OUT INTERNO'` en cada corrida — el mismo anti-patrón de nombre-hardcodeado-como-dato que el propio proyecto ya había identificado y resuelto una vez (el comentario "es_referencia_generada (0/1, ex REFOUT)" en asegurar_columnas_control_idioma documenta que esa columna existe justamente para reemplazar un antiguo check nombre='REFOUT'). Cualquier usuario renombrando ese conector (traducción, corrección de typo) borraba en silencio TODA la distribución de referencia del frame, sin error ni aviso.
2026-09-10T05:30 modelo.py: nueva columna `conector.es_salida_referencia_frame` (booleana, por conector puntual — mismo criterio que `es_entrada_referencia`, un equipo DISTRIBUIDOR_FRAME podría tener más de un conector y sólo uno es la salida real de backplane). Semilla de migración única a partir de `UPPER(nombre)='REF OUT INTERNO'` (mismo criterio que la semilla de `es_referencia_generada`/REFOUT) para no romper instalaciones que ya tengan un conector con ese nombre exacto. `sincronizar_referencia_virtual_frame()` deja de comparar por nombre, ahora filtra por `c.es_salida_referencia_frame=1`. Nuevos `Modelo.establecer_es_salida_referencia_frame_conector(id_conector, valor)` (dispara resync inmediato, reutiliza `_resync_referencia_virtual_si_aplica`) y `Modelo.devolver_es_salida_referencia_frame_conector(id_conector)`.
2026-09-10T05:30 conectores_ui.py: `_DialogoConector`, sección "Referencia", gana un segundo checkbox "Es la salida de referencia interna del frame (REF OUT INTERNO)" al lado del de entrada — se guarda en `run_and_destroy()` junto con el otro. El nombre visible del conector puede ser cualquier cosa a partir de ahora; lo que importa es este checkbox.
2026-09-10T05:30 Validación: (1) ast.parse + pyflakes sobre modelo.py y conectores_ui.py contra baseline de main: cero advertencias nuevas; (2) smoke test funcional pensado específicamente para el caso reportado — se arma una base SQLite "vieja" con SQL crudo (sin pasar por Modelo, para no disparar la migración antes de tiempo) con un conector ya llamado "REF OUT INTERNO", se corre `asegurar_columnas_control_idioma()` por primera vez contra esa base y se confirma que la semilla retroactiva lo marcó solo; se activa `es_entrada_referencia` en un módulo, se confirma que se crea la virtual usando el checkbox (no el nombre); SE RENOMBRA el conector de salida a un texto arbitrario y se vuelve a llamar `sincronizar_referencia_virtual_frame()` — la virtual sigue existiendo (antes de este fix, hubiera desaparecido); por contraste, destildar a mano el checkbox de salida sí borra la virtual (comportamiento correcto y deliberado, a diferencia del renombre accidental); (3) smoke test GTK bajo Xvfb: `_DialogoConector` real con ambos checkboxes precargados correctamente contra un conector con `es_salida_referencia_frame=1` en la base. **Sigue pendiente:** correr contra `db.db` real; regla_logica REF1→REFOUT sigue manual. APP_VERSION → 1.20260910053000.
2026-09-10T06:15 FEATURE nueva (pedido de Fede, misma sesión): "Editar matriz" en el Diagrama de Conexiones estaba restringido a equipos con rol_senal='ENRUTADOR' (o tipo_equipo.nombre=='MATRIZ' como fallback) — un candado puramente de UI, no de datos. Se confirmó investigando el código: `_DialogoRuteoMatriz`, `Modelo.guardar_ruteo_matriz()`/`devolver_ruteo_matriz()`/`existe_configuracion_matriz()` ya eran completamente genéricos (sólo necesitan que el equipo tenga conectores IN/OUT, que grafo_diagrama_ui.py ya arma para TODOS los equipos del diagrama, no sólo los ENRUTADOR), y graph_impact.py (Análisis de Impacto) ya lee `matriz_ruteo` sin filtrar por rol_senal tampoco. Se habilita entonces "Editar matriz" para cualquier equipo con al menos una entrada y una salida.
2026-09-10T06:15 ruteo_interno_diagrama_ui.py: `_editar_ruteo_matriz_click()` deja de exigir rol_senal='ENRUTADOR'/tipo=='MATRIZ', ahora sólo valida que el nodo tenga `in` y `out` no vacíos (con mensaje explicando cuántas entradas/salidas tiene si no califica). `_toggle_conexion_interna()` gana una rama nueva: si el equipo no es PATCHERA/DDV/ENRUTADOR pero YA tiene un ruteo guardado (por haber pasado por "Editar matriz"), también se puede visualizar con "Conexión interna" — a propósito NO auto-abre el diálogo de configuración en este caso (a diferencia de la rama ENRUTADOR), para no sorprender al usuario abriendo un diálogo de ruteo en cualquier equipo con sólo tocar "Conexión interna" sin haber pasado antes por "Editar matriz" explícitamente.
2026-09-10T06:15 diagrama_conexiones_ui.py: tooltips de "🔀 Conexión interna" y "✏️ Editar matriz" actualizados para reflejar que ya no hace falta que el equipo sea una matriz dedicada.
2026-09-10T06:15 NOTA IMPORTANTE, no resuelta en esta entrega (a confirmar con Fede si la quiere): `senal_propagation.py` (motor de "Riesgo de señal"/formato de señal, plan_riesgo_senal_audio.md — DISTINTO del Análisis de Impacto) sigue teniendo su propio gate `elif rol == "ENRUTADOR":` para decidir si usar el ruteo guardado al propagar formato de señal internamente en un equipo. Un equipo con un ruteo configurado vía "Editar matriz" pero SIN rol_senal=ENRUTADOR va a funcionar correctamente en el Análisis de Impacto (ya genérico) pero el motor de Riesgo de señal lo va a seguir tratando como "sin aristas internas" (no propaga formato de entrada a salida). Unificar esto significa tocar un motor distinto con datos ya en producción (equipos con matriz_ruteo residual de un rol_senal viejo que hoy se ignoran a propósito) — se dejó afuera de esta entrega hasta confirmar si hace falta.
2026-09-10T06:15 Validación: (1) ast.parse + pyflakes sobre ruteo_interno_diagrama_ui.py y diagrama_conexiones_ui.py contra baseline de main: cero advertencias nuevas; (2) smoke test aislado del control de flujo de `_editar_ruteo_matriz_click()` con una clase stub mínima (sin GTK real): equipo genérico tipo "CAMARA" sin rol_senal → habilita editar matriz; equipo sin salidas → rechazado con mensaje claro (no revienta); sin selección → mensaje esperado; (3) smoke test funcional contra SQLite sintética: un equipo tipo "CAMARA-GENERICA" (rol_senal≠ENRUTADOR) puede guardar/leer un ruteo vía `Modelo.guardar_ruteo_matriz()`/`devolver_ruteo_matriz()`/`existe_configuracion_matriz()` sin ningún error, y `GraphImpactAnalyzer` lo lee correctamente en `self._matriz_ruteo` sin filtrar por rol; (4) import real bajo Xvfb de ambos módulos + identidad de reexport `cabledoc.DiagramaConexiones`. APP_VERSION → 1.20260910061500.
2026-09-11T04:00 Fase 0-2 de plan_integracion_cabledoc_v3.md ("core/ compartido, versión real" + "UI desktop"): reorganización del repo en core/ (sin GTK/Kivy, fuente única de verdad) + ui_gtk/ (todo lo GTK-específico) + data/ (fuera de git de acá en más, ver data/README.md), como paso previo a compartir modelo.py real (7.294 líneas, no una copia congelada) con el proyecto mobile Kivy. git mv usado en todos los casos para preservar historial de cada archivo. Cero cambios de lógica de negocio: es un *move* + reescritura mecánica de imports, mismo criterio que ya usó el proyecto en el refactor de cabledoc.py (plan_refactor_cabledoc.md).
2026-09-11T04:00 core/: movidos tal cual modelo.py, i18n.py, graph_impact.py, risk_engine.py, escenario_engine.py, senal_propagation.py, senal_estado.py, riesgo_analogico.py, diagnostico_falla.py, signal_risk.py (los 10 de plan_integracion_cabledoc_v3.md §4 Fase 1) + senal_visual.py (hallazgo de esta entrega: dependencia real de diagnostico_falla.py — VisualizadorSenal —, libre de GTK, que el plan no había listado explícitamente; documentado como corrección al plan, no como desviación silenciosa). Agregado core/__init__.py (paquete vacío) y core/logger_cabledoc.py, traído del lado mobile: no existe en desktop (Modelo._query/_exec de desktop nunca llaman a log_error, a diferencia del modelo.py de mobile) — se incorpora a core/ por ser genérico y sin dependencias GTK/Kivy, pero la integración real (que core/modelo.py llame a log_error) queda pendiente de decisión, no se tocó modelo.py de ese lado en esta entrega.
2026-09-11T04:00 core/modelo.py: único cambio de comportamiento real de toda la Fase 1 (todo lo demás es *move* puro). DB_PATH/IMG_DIR/MANUALES_DIR/PICON_DIR/SCHEMA_SQL_PATH se calculaban relativos a la carpeta de modelo.py (raíz del proyecto hasta ahora). Al mover modelo.py a core/, esa carpeta pasa a ser la raíz del repo (cabledoc/), un nivel arriba de core/ — data/ es hermana de core/, no hija (ver árbol de plan_integracion_cabledoc_v3.md §3). Se corrigió subiendo un nivel más (_REPO_ROOT = dirname(dirname(__file__))) para seguir apuntando a los mismos datos de siempre, ahora bajo data/database/db.db, data/imagen/, data/manuales/, data/picon/, data/schema_db.sql.
2026-09-11T04:00 core/graph_impact.py: parche cosmético de robustez de plan_integracion_cabledoc_v3.md §3/§4 Fase 1 — el import de graphqlite.graph.Graph, la instanciación Graph(":memory:") y la llamada a insert_graph_bulk() quedan envueltos para tolerar que el paquete no esté instalado (Graph=None si falla el import; self._g queda en None y se saltea el bulk insert). Cero cambios de resultado: ningún cálculo real de simular_falla_equipo()/simular_desconexion()/simular_escenario() depende de self._g, todo corre sobre el espejo Python puro self._adj, sin tocar. cypher_console.py (único consumidor real de self._g/Cypher) queda fuera de core/, sin cambios, tal como especifica el plan.
2026-09-11T04:00 ui_gtk/: movidos tal cual los ~45 archivos GTK restantes de desktop (cabledoc.py, pantallas_*.py, *_ui.py, *_diagrama_ui.py, cypher_console.py, diagrama_personalizado.py, acerca_de.py) + assets/ + help/. Reescritos mecánicamente todos los imports de los 12 módulos de core (from X import → from core.X import), incluyendo imports diferidos/indentados dentro de try/except y funciones (ej. cabledoc.py línea 54/943, equipos_ui.py línea 42) que un sed anclado a ^from no captura, y los 3 usos de __import__("modelo").DB_PATH en panel_arbol_ui.py (reescritos a __import__("core.modelo", fromlist=["DB_PATH"]).DB_PATH) + el import modelo as _modelo del bloque __main__ de cabledoc.py.
2026-09-11T04:00 ui_gtk/cabledoc.py y ui_gtk/cypher_console.py: agregado bootstrap de sys.path (inserta la raíz del repo, padre de ui_gtk/, al principio de sys.path si no está ya) antes de cualquier import de gi/core — necesario para que from core.X import Y resuelva sin importar cómo se lance el archivo (python3 ui_gtk/cabledoc.py, ./lanzar.sh, etc.), ya que Python sólo agrega automáticamente la carpeta del propio script a sys.path, no su padre. APP_VERSION → 1.20260911040000.
2026-09-11T04:00 data/: nueva carpeta, fuera de git de acá en más (agregar a .gitignore antes de mergear — ver data/README.md, no se agregó el .gitignore en esta entrega para no decidir por Papi/Fede si se hace ahora o en un commit aparte). Contiene schema_db.sql (sí es código, se queda en git) + database/, imagen/, manuales/, picon/ con .gitkeep vacíos — los datos reales de producción de Papi/Fede no se incluyen en esta entrega, se siguen sincronizando por rclone/Gdrive (Fase 6 del plan) por fuera de este repo.
2026-09-11T04:00 requisitos.txt → requirements-desktop.txt (mismo contenido, sólo renombrado según el árbol de plan_integracion_cabledoc_v3.md §3 — requirements-mobile.txt todavía no existe, se agrega junto con ui_kivy/ en la próxima entrega). lanzar.sh actualizado: .venv/bin/python cabledoc.py → .venv/bin/python ui_gtk/cabledoc.py.
2026-09-11T04:00 Validación de esta entrega (mismo checklist del proyecto, adaptado a un *move* multi-archivo en vez de una entrega de una sola pantalla): (1) ast.parse sobre los 58 archivos .py de core/+ui_gtk/: OK; (2) py_compile sobre los mismos 58: OK; (3) pyflakes comparado contra el baseline de main (git show main:archivo.py por archivo, mismo criterio que el resto del proyecto): CERO advertencias F821 (nombres indefinidos) nuevas en ningún archivo — las únicas categorías nuevas son "imported but unused" esperadas por el propio patrón de reexport de cabledoc.py (ya documentado en el changelog de las Entregas 1-10 del refactor) y por el cambio de nombre del import (ej. "core.modelo.IMG_DIR imported but unused" reemplaza lo que antes decía "modelo.IMG_DIR imported but unused"), sin ningún hallazgo real nuevo. (4) sandbox con acceso de red: instalados gir1.2-gtk-3.0, gir1.2-rsvg-2.0, python3-gi-cairo, xvfb, sqlite3 y pip graphqlite; smoke test real bajo Xvfb contra una base SQLite generada desde data/schema_db.sql: `import core.modelo` resuelve DB_PATH a data/database/db.db (confirma el fix de rutas de core/modelo.py); `import cabledoc` desde ui_gtk/ resuelve todos los `from core.X import` sin excepciones (incluido cypher_console.py, que importa graphqlite a nivel de módulo — con el paquete instalado no hace falta el guard, y sin él el plan ya preveía que esa ventana queda fuera del alcance funcional, no del import); identidad de reexport confirmada (`core.modelo.Modelo is cabledoc.Modelo`); `VentanaPrincipal()` instanciada sin excepciones contra la fixture vacía (recargar() corrió su ciclo completo); `GraphImpactAnalyzer` de core/graph_impact.py instanciado con GraphQLite real disponible. (5) repetido con graphqlite desinstalado: `from core.graph_impact import Graph` da `None` (rama ImportError real, no sólo la del import exitoso) y `GraphImpactAnalyzer(...)` se sigue instanciando sin excepciones — confirma que el parche cosmético de la Fase 1 funciona en ambos sentidos. Pendiente para la próxima sesión: arrancar la Fase 3 (ui_kivy/, todavía sin empezar en este commit) y el resto del roadmap del plan.
2026-09-11T05:00 Eliminación de GraphQLite y la Consola Cypher del desktop (pedido explícito de Papi, cambia el criterio de plan_integracion_cabledoc_v3.md §3/§6, que planeaba solo "envolver" el import — ahora se retira por completo): core/graph_impact.py sin import de graphqlite.graph.Graph ni try/except ImportError asociado; el flag self._g (Optional[Graph]) se reemplaza por self._grafo_construido: bool, usado en los 7 puntos que antes chequeaban "if self._g is None" (5 simular_*/puntos_finales/totales) más esta_construido()/invalidar() — mismo comportamiento (RuntimeError si no se llamó construir_grafo() antes), sin ningún cambio de resultado en ningún cálculo real (ya corrían sobre self._adj, nunca sobre self._g). Se eliminó también la lista `nodes` y la llamada self._g.insert_graph_bulk(nodes, edges) en construir_grafo(): código muerto que solo alimentaba a GraphQLite, sin ningún otro consumidor. Todos los docstrings/comentarios de core/graph_impact.py, core/escenario_engine.py, core/risk_engine.py, core/senal_propagation.py que mencionaban GraphQLite/Cypher como justificación de diseño o causa de fallback fueron reescritos para reflejar la arquitectura real (espejo en Python puro, sin dependencias externas).
2026-09-11T05:00 ui_gtk/cypher_console.py eliminado del repo (git rm, 1880 líneas) junto con su único consumidor: ui_gtk/cabledoc.py sin el import `from cypher_console import CypherConsole`, sin la entrada de menú "🔮 Consola Cypher (GraphQLite)…" (menú Diagramas), sin el botón de acceso rápido "🔮 Consola Cypher" (pantalla de inicio) y sin el método _abrir_cypher(). ui_gtk/escenario_ui.py: mensaje de error de _esc_activar_modo() ya no le pide al usuario que revise si graphqlite está disponible (causa que dejó de ser posible), reemplazado por un mensaje genérico sobre equipos/conexiones cargadas en la base. core/i18n.py: quitadas las ~17 claves de traducción exclusivas de la Consola Cypher (menú, ventana, botones del editor de queries) más la entrada de acceso rápido. Documentación actualizada para reflejar la eliminación: README.md (funcionalidades, tabla de módulos, requisitos, árbol de archivos), ui_gtk/help/TUTORIAL_funcionalidades_avanzadas.md (sección 9 "Consola Cypher" eliminada completa, sección 10 renumerada a 9, tabla comparativa y texto de la sección 8 corregidos), ui_gtk/help/TUTORIAL_riesgo_de_senal.md (bullet de Consola Cypher quitado), context.md y PROGRESS.md (bullets de arquitectura/stack actual, sin tocar entradas de historial). requirements-desktop.txt no tenía a graphqlite listado (se instalaba manualmente en cada sesión de validación, nunca declarado como dependencia formal), así que no requirió cambios. APP_VERSION → 1.20260911050000.
2026-09-11T05:00 Validación de esta entrega: (1) ast.parse + py_compile sobre los 7 archivos .py tocados (core/graph_impact.py, core/escenario_engine.py, core/risk_engine.py, core/senal_propagation.py, core/i18n.py, ui_gtk/cabledoc.py, ui_gtk/escenario_ui.py): OK; (2) pyflakes comparado línea por línea contra el baseline (checkpoint previo a esta entrega, ya con Fase 0-2 aplicada): cero advertencias nuevas en ningún archivo — mismo set de "imported but unused"/duplicados de i18n preexistentes, solo desplazados de línea por las líneas removidas en cabledoc.py; graph_impact.py y risk_engine.py sin ninguna advertencia en ambos casos; (3) sandbox SIN graphqlite instalado (escenario ideal para esta prueba, a diferencia de sesiones anteriores que sí lo tenían): fixture SQLite generada desde data/schema_db.sql (49 tablas) vía sqlite3 estándar de Python (no se pudo instalar el CLI sqlite3 por 404 del repo de Ubuntu esta sesión, se usó el módulo de la librería estándar en su lugar, equivalente); GraphImpactAnalyzer instanciado real e importado con éxito (confirmado 'graphqlite' ausente de sys.modules durante todo el ciclo), ciclo completo construir_grafo() → esta_construido()==True → totales()==(0,0) contra una base vacía → invalidar() → esta_construido()==False → RuntimeError esperado en la siguiente llamada a totales(); Escenario.asegurar_grafo() (escenario_engine.py, consumidor de graph_impact.py) también confirmado sin graphqlite en sys.modules; core.i18n cargó sus 406 claves sin error de sintaxis. **No se corrió el smoke test bajo Xvfb con GTK real esta vez**: context.md del propio repo indica explícitamente que Fede prefiere correr esas pruebas de Xvfb/GTK por su cuenta y no que Claude las intente — a diferencia de la nota de la entrega anterior (Fase 0-2), que sí las había corrido; señalado como discrepancia a confirmar con Papi/Fede, no resuelto unilateralmente en esta entrega. Se cubrió en su lugar con pruebas de lógica pura (sin gi/GTK) sobre los dos módulos que consumían GraphQLite directa o indirectamente.
2026-09-16T05:00 Fix reportado por Papi: "el mobile no muestra los SVG". Investigado sobre main (5ac63de, ya con Fase 3.4/símbolos vectoriales mergeada vía PR #43). La funcionalidad de rasterizado SVG en mobile YA estaba implementada en ui_kivy/widgets_base.py (crear_textura_imagen_svg para fondos de equipo/frame, crear_textura_simbolo para símbolos de conector), pero nunca pudo funcionar en un dispositivo real: requirements-mobile.txt dejaba svglib/reportlab comentadas, documentadas como "opcionales/último recurso" con el razonamiento de que sólo hacen falta para el caso raro de MEDIR un SVG sin viewBox/width/height explícitos (Modelo._dimensiones_svg_sin_gi resuelve el caso típico con puro regex, sin dependencias). Ese razonamiento es correcto para medir pero incorrecto para DIBUJAR: kivy.core.image no soporta SVG en absoluto (a diferencia de GdkPixbuf en GTK), así que el rasterizado real pasa siempre por svglib+reportlab, sin ningún atajo de regex. Con las dependencias comentadas, cualquier instalación real siguiendo requirements-mobile.txt tal cual disparaba un ImportError atrapado en silencio por el except Exception de ambas funciones, la textura quedaba None, y VisorImagenZoom mostraba el mismo cartel genérico "Sin imagen asignada al equipo/conector" que si el equipo nunca hubiera tenido imagen — indistinguible en pantalla del caso real de "no hay imagen", por eso Papi/Fede lo veían como que la función "no existía".
2026-09-16T05:00 requirements-mobile.txt: svglib y reportlab dejan de estar comentadas/opcionales, ahora son dependencias obligatorias. HALLAZGO NUEVO de esta misma entrega (confirmado probando el pipeline completo en sandbox de escritorio): svglib+reportlab solas no alcanzan — reportlab 4.x ya no trae backend de rasterización propio (el viejo _rl_renderPM, C precompilado sin cairo, fue discontinuado) y renderPM.drawToFile ahora exige el paquete rlPyCairo, que a su vez compila pycairo (extensión C) contra libcairo del sistema vía pkg-config. Se agregó rlPyCairo>=0.3.0 al requirements. Funcionó sin problema en este sandbox (que trae libcairo-dev), pero queda SIN CONFIRMAR si pycairo compila limpio en Pydroid 3 real — Android normalmente no expone libcairo-dev a pip por defecto, es exactamente el riesgo que el comentario anterior del archivo ya sospechaba, ahora con el nombre correcto del paquete a probar. Comentario del archivo reescrito completo para dejar esta distinción (medir vs. dibujar) y este hallazgo documentados para la próxima sesión.
2026-09-16T05:00 ui_kivy/widgets_base.py: crear_textura_imagen_svg y crear_textura_simbolo ahora llaman a core.logger_cabledoc.log_error(origen, exc) en su except Exception antes de devolver None — antes el fallo (típicamente ImportError por dependencias faltantes) se perdía por completo, sin ningún rastro en log.txt ni en pantalla. VisorImagenZoom.set_imagen distingue ahora dos mensajes de UI donde antes había uno solo: "Sin imagen asignada al equipo/conector" (sin cambios, cuando no hay ruta o el archivo no existe) vs. el nuevo "No se pudo mostrar la imagen SVG (revisar log.txt — puede faltar svglib/reportlab/rlPyCairo)" (cuando la ruta es un .svg existente pero crear_textura_imagen_svg devolvió None) — mismo contrato de "nunca romper el render por un fallo puntual" de antes, sólo que ahora el motivo es diagnosticable en vez de idéntico al caso de "no hay imagen".
2026-09-16T05:00 PROGRESS_INTEGRACION_MOBILE.md: Fase 3.3 pasa de [ ] a [/] (en progreso, no cerrada) con el detalle de este hallazgo y el hallazgo de rlPyCairo/pycairo como bloqueo real pendiente de la prueba en dispositivo. Nueva entrada en "Latest Blockers/Discoveries" con el reporte de Papi, la investigación y el fix, para trazabilidad.
2026-09-16T05:00 Validación: (1) ast.parse + py_compile sobre requirements-mobile.txt (N/A, no es .py) y ui_kivy/widgets_base.py: OK; (2) pyflakes sobre ui_kivy/widgets_base.py comparado contra baseline de main (git stash): cero advertencias nuevas, único cambio preexistente desplazado de línea (BooleanProperty imported but unused); (3) sandbox con acceso de red: instalados svglib, reportlab y rlPyCairo (compiló pycairo sin problema, confirma que este sandbox de escritorio SÍ trae libcairo-dev — no es evidencia de que Pydroid 3 también); pipeline real probado standalone (svg2rlg + renderPM.drawToFile) contra un SVG de prueba: falla con "cannot import desired renderPM backend rlPyCairo" sin rlPyCairo instalado, funciona y genera un PNG válido (150x75, confirmado con los bytes de cabecera) con rlPyCairo instalado — confirma el hallazgo antes de escribirlo en el changelog, no es especulación; (4) smoke test funcional con Kivy 2.3.1 real en modo headless (KIVY_UNITTEST=1, sin necesitar Xvfb ni ventana) contra ui_kivy/widgets_base.py real: (a) crear_textura_imagen_svg con las 3 dependencias instaladas devuelve una Texture válida y no loguea nada; (b) con svglib/reportlab parcheados para fallar (simulando el dispositivo sin las dependencias), devuelve None y log.txt registra la entrada "(crear_textura_imagen_svg)" con el traceback real del ImportError simulado; (c) VisorImagenZoom.set_imagen(None) muestra "Sin imagen asignada al equipo/conector"; (d) VisorImagenZoom.set_imagen(<ruta .svg>) con el mismo fallo simulado muestra el nuevo mensaje distinto "No se pudo mostrar la imagen SVG (revisar log.txt...)" — los 4 casos confirmaron el comportamiento esperado; (5) git apply --check del diff contra un stash limpio de main: OK. Pendiente para Fede: la prueba de 15 minutos en Pydroid 3 real instalando requirements-mobile.txt completo (en particular confirmar si rlPyCairo/pycairo compilan ahí); si no compilan, evaluar como próxima entrega un rasterizador propio basado sólo en Pillow (ya obligatoria) para el subconjunto de SVG que exporta la herramienta vectorial del proyecto, sin depender de cairo. APP_VERSION → 1.20260916050000 (repo unificado desktop+mobile, se bumpea aunque esta entrega sólo tocó archivos mobile/requirements-mobile.txt, a pedido explícito de Papi).
2026-09-16T06:00 Fase 3.5 (plan_svg_pygame_nanosvg_v1.md, adjuntado por Fede junto con test_svg_pygame.py corregido y PROGRESS.md de la investigación): ruta de rasterizado SVG en mobile vía pygame/SDL_image (NanoSVG), complementaria/primaria sobre la cadena svglib+reportlab+rlPyCairo de la entrega 2026-09-16T05:00. Motivo: rlPyCairo exige compilar pycairo contra libcairo del sistema, algo NO confirmado en Pydroid 3 (Android normalmente no expone libcairo-dev a pip); pygame en cambio ya fue CONFIRMADO en dispositivo real (Pydroid 3, pygame 2.6.1 clásico —no pygame-ce—, SDL_image (2, 6, 3)) rasterizando 7 SVG reales del proyecto (fondos de equipo/plano + símbolos tipo conector) sin necesitar compilar nada. También confirmado en el mismo dispositivo: `transform="scale(N)"` en el nodo raíz NO sirve para pedir más resolución (SDL_image/NanoSVG calculan el canvas a partir de width/height/viewBox, no de transforms); la única forma que funciona es sobreescribir width/height directamente, confirmado con factor x4 exacto y proporcional sobre los 7 SVG reales.
2026-09-16T06:00 ui_kivy/svg_raster_pygame.py (nuevo): módulo standalone según contrato del plan §3.2 — `svg_disponible()` (detección una sola vez por proceso, nunca llama pygame.init() completo, sólo inicializa el subsistema de imagen con driver 'dummy' si algún build headless lo exige) y `rasterizar_svg_a_png(ruta_svg, ruta_png_destino, ancho_px=None)`. Se agregaron además `rasterizar_svg_bytes`/`rasterizar_svg_bytes_a_png_bytes` (no estaban en el contrato original del plan) para integrarse en memoria vía BytesIO, mismo patrón que ya usaban crear_textura_imagen_svg/crear_textura_simbolo con el buffer de reportlab — evita escribir a disco en el caso común. Nunca lanza excepción: cualquier fallo (pygame no instalado, SVG con features que NanoSVG no soporta, etc.) devuelve None/False, el llamador cae a su propio fallback.
2026-09-16T06:00 ui_kivy/widgets_base.py: crear_textura_simbolo y crear_textura_imagen_svg prueban ahora pygame PRIMERO (vía svg_raster_pygame) y caen a la cadena svglib+reportlab+rlPyCairo existente sólo si pygame no está disponible o falla puntualmente para un SVG dado (p.ej. <text> o gradientes que NanoSVG no soporta — auditoría de catalogo_simbolo_conector.svg_fragmento reales sigue pendiente, ver PROGRESS.md de la investigación). Mismo contrato de "nunca romper el render por un fallo puntual" de antes. Hallazgo colateral positivo: pygame preserva el canal alfa real del SVG al guardar PNG (confirmado con PIL: fondo transparente, relleno opaco), evitando el workaround de bg=0x000000/configPIL de la ruta reportlab. Mensaje de UI de VisorImagenZoom (_MSG_SVG_NO_RENDERIZADO) actualizado para mencionar pygame como primera dependencia a revisar.
2026-09-16T06:00 requirements-mobile.txt: agregado pygame>=2.0.0 como dependencia PRIMARIA (Pydroid 3 suele traerlo instalado de fábrica). svglib/reportlab/rlPyCairo quedan documentadas como dependencias de FALLBACK (para cuando pygame no esté disponible o falle con un SVG puntual), no eliminadas — no se descarta la Fase 3.3-bis (Pillow puro) planteada en PROGRESS_INTEGRACION_MOBILE.md, sólo se pospone su necesidad porque pygame ya cubre el caso confirmado en dispositivo real.
2026-09-16T06:00 Validación: (1) ast.parse + py_compile sobre ui_kivy/svg_raster_pygame.py y ui_kivy/widgets_base.py: OK; (2) pyflakes comparado contra baseline de main (clon fresco de fschpp/CabledocDesktop, commit ad0ea488): CERO advertencias nuevas (el único warning de widgets_base.py, BooleanProperty imported but unused, ya estaba en el baseline); (3) pruebas funcionales de svg_raster_pygame.py standalone contra pygame 2.6.1 real (sandbox de escritorio, SDL_image (2,0,5) — versión distinta a la del dispositivo pero mismo motor NanoSVG): SVG sintético, escalado a ancho_px arbitrario (natural 100x100 -> 400x400 exacto), fragmento tipo símbolo de conector (sin tag <svg> exterior, envuelto igual que crear_textura_simbolo), archivo->PNG en disco, SVG inválido -> None sin excepción, y 3 variantes de formato de width/height (comillas simples, orden invertido, sin atributos — sólo viewBox) -> las 3 escalan proporcionalmente bien; (4) smoke test funcional REAL bajo Xvfb con Kivy 2.3.1 instalado (import real de ui_kivy/widgets_base.py, no un stub): crear_textura_simbolo y crear_textura_imagen_svg devuelven Texture de Kivy válidas (colorfmt='rgba', tamaño correcto) usando la ruta pygame; (5) simulando pygame no disponible (svg_raster_pygame._SVG_DISPONIBLE=False a mano) sin svglib/reportlab/rlPyCairo instalados: crear_textura_simbolo cae al diagnóstico existente (_diagnosticar_dependencias_svg) y devuelve None sin excepción, igual que antes de esta entrega; (6) git apply --check del diff (3 archivos: 1 nuevo + 2 modificados) contra un clon fresco de main: OK. **Pendiente para Fede/Papi, no cerrable desde acá:** correr esta entrega en Pydroid 3 real para confirmar que pygame ya viene instalado sin necesitar compilar nada (a diferencia de rlPyCairo); y los ítems ya abiertos en el PROGRESS.md de la investigación — auditar catalogo_simbolo_conector.svg_fragmento reales por <text>/gradientes, y verificar fidelidad visual de grassvalley_switcher_kayak_hd.svg (mancha blanca redondeada notada en la miniatura, podría ser texto/logo perdido por NanoSVG). APP_VERSION -> 1.20260916060000.
2026-09-16T07:00 PLAN_CIERRE_ROADMAP_MOBILE.md (documento de Papi, adjuntado en esta sesión, no versionado en el repo): ítem 1 ("Fix: integración de Vista Previa") resultó ya resuelto — al re-clonar `main` apareció el merge de PR #46 (`8197e8d`/`aa409fe`, "se agrego el boton vista previa en diagrama de conexiones que faltaba") hecho por Fede en paralelo, con un enfoque equivalente al planeado (mismo orden de prioridad Diagnóstico → Escenario → Vista Previa en `_CanvasDiagrama.on_touch_down`, sin exclusión mutua). No requirió cambios adicionales. Se continuó con el ítem 2 del plan (Fase 4 — Consistencia visual ABM).
2026-09-16T07:00 ui_kivy/pantallas_racks.py: los 4 diálogos restantes de Fase 4 envueltos en `seccion_tarjeta()` (mismo patrón ya usado en pantallas_cables.py/pantallas_conectores.py/pantallas_equipos.py, sin backend nuevo). `DialogoRack` ("Datos del rack", 1 sección) y `DialogoFrame` ("Datos del frame", 1 sección) sin cambios de estructura, sólo el envoltorio. `DialogoPosicionRack` ("Datos de la posición", 1 sección — rack/orificio/UR/tipo dispositivo/equipo/frame no separan en secciones adicionales, es un único formulario de asignación). `DialogoSlot` se dividió en DOS secciones ("Datos del slot": nombre/equipo/imagen: y "Rectángulo en imagen": X/Y/ancho/alto), único de los 6 diálogos de esta entrega con más de una sección lógica clara.
2026-09-16T07:00 ui_kivy/pantallas_salas.py: `DialogoRackPorSala` ("Asignación Rack-Sala") y `DialogoEquipoNoRackSala` ("Equipo suelto en sala") envueltos en `seccion_tarjeta()`, 1 sección cada uno (sólo 2 selectores por diálogo, no amerita más de una tarjeta).
2026-09-16T07:00 Íconos usados (catálogo existente en ui_kivy/assets/iconos/, sin agregar ninguno nuevo): "racks" (Rack, Posición en Rack), "frame" (Frame, Slot), "imagen" (Rectángulo en imagen de Slot), "ubicacion" (Rack por Sala, Equipo suelto en sala) — no existe un ícono "rack"/"sala" dedicado, se usó el más cercano del catálogo real en vez de inventar un nombre de archivo.
2026-09-16T07:00 Validación: (1) ast.parse + py_compile sobre ambos archivos: OK; (2) pyflakes comparado contra baseline (git stash, HEAD=aa409fe): cero advertencias nuevas en los dos archivos (los preexistentes — Label/sp/mostrar_error sin usar en pantallas_racks.py, TextInput/sp sin usar en pantallas_salas.py — ya estaban antes de este cambio); (3) fixture SQLite armada desde data/schema_db.sql (49 tablas) vía el módulo sqlite3 de Python + `Modelo.asegurar_tablas_plano()` corrida una vez (agrega `sala.id_plano`/`.poligono`, requerido por `devolver_todos_rack_por_sala()` — no estaba en el schema base, es una migración idempotente aparte; hallazgo de esta sesión, no un bug introducido por el cambio) + datos mínimos sembrados con los métodos reales de Modelo (`alta_marca`, `alta_rack`, `agregar_frame`, `agregar_slot`, `alta_sala`, `alta_rack_por_sala`, `alta_dispositivo_en_rack`); (4) smoke test real con Kivy 2.3.1 bajo Xvfb: los 6 diálogos abren sin excepción (`seccion_tarjeta`/`Tarjeta` construyen bien sobre datos reales); `DialogoRack`/`DialogoPosicionRack`/`DialogoFrame`/`DialogoSlot`/`DialogoRackPorSala` en modo edición confirmaron que sus campos clave quedan poblados igual que antes del cambio (nombre/rack/sala, según corresponda); `DialogoEquipoNoRackSala` probado en modo "nuevo" (sin fila existente) — 12/12 checks OK. Fixture y smoke test borrados al terminar (no se commitean), siguiendo el criterio de limpieza del proyecto.
2026-09-16T07:00 cabledoc.py: sólo APP_VERSION → 1.20260916070000 (repo unificado desktop+mobile, se bumpea aunque esta entrega sólo tocó archivos mobile, mismo criterio ya aplicado el 2026-09-16T05:00).
2026-09-16T08:00 Fix reportado por Papi (traceback real al probar la entrega de Fase 4 en desktop, no relacionado a PLAN_CIERRE_ROADMAP_MOBILE.md): `TypeError: Modelo.devolver_todas_las_conexiones() takes from 0 to 1 positional arguments but 2 were given` al abrir Conexiones desde la ficha de Equipo en mobile (`pantallas_equipos.py:_ver_conexiones_equipo` -> `ConexionesListado(id_equipo=...)` -> `cargar_datos()` en pantallas_conexiones.py:36, que llama a `Modelo.devolver_todas_las_conexiones(self.id_cable_filtro, self.id_equipo_filtro)` pasando dos posicionales). Investigado sobre main (cd0e751, ya con el merge de PR #47 de Fase 4): el core nunca soportó filtrar por id_equipo en este método — `devolver_todas_las_conexiones(id_cable=None)` sólo aceptaba id_cable, tal cual sigue llamándolo GTK (`cables_conexiones_ui.py:721`, sin cambios). Descartado reusar `Modelo.devolver_conexiones_de_equipo(id_equipo)` (la función que sí filtra por equipo, consumida por `ConexionesDeEquipoVentana` en GTK): usa la vista `CONEXIONES_AMBOS_EXTREMOS` con un esquema de 14 columnas completamente distinto (pensado para mostrar ambos extremos de cada conexión), incompatible con el mapeo de columnas que ya usa `ConexionesListado.cargar_datos()` (esquema de 9 columnas de la vista `CONEXIONES`, compartido con GTK). El código de pantallas_conexiones.py ya estaba escrito asumiendo que el core aceptaría ambos filtros -- gap de sincronización viejo, anterior a esta sesión, recién detectado porque nadie había abierto ese botón puntual de mobile contra datos reales hasta ahora.
2026-09-16T08:00 core/modelo.py: `devolver_todas_las_conexiones(id_cable=None, id_equipo=None)` -- gana el segundo filtro opcional (columna `id_equipo`, ya presente en la vista `CONEXIONES`), combinables con AND si se pasan los dos. Mismo esquema de 9 columnas de siempre en todos los casos, así que no hace falta tocar el mapeo de columnas en ningún consumidor (GTK ni mobile). Backward-compatible: la única llamada de GTK sigue pasando un solo posicional (id_cable) y no se ve afectada. Docstring nuevo aclarando la diferencia con `devolver_conexiones_de_equipo()` (vista distinta, no confundir). Mismo criterio ya usado en la entrega de Fase 4-adyacente `devolver_cables_de_equipo()` (variante filtrada por equipo, mismo esquema que su contraparte sin filtro) -- no fue necesario, en este caso, crear una función nueva porque devolver_todas_las_conexiones() ya tenía la forma condicional (if id_cable: ... else: ...) lista para extender con un segundo filtro sobre la misma vista.
2026-09-16T08:00 Validación: (1) py_compile sobre core/modelo.py: OK; (2) pyflakes comparado contra el baseline de main (cd0e751, antes del cambio): cero advertencias nuevas; (3) fixture SQLite real generada desde data/schema_db.sql (sqlite3 estándar de Python) con 2 equipos, 2 conectores y 1 cable con sus 2 conexiones: confirmados los 4 call sites reales del repo contra datos reales -- `devolver_todas_las_conexiones(None, id_equipo)` (el caso roto, pantallas_conexiones.py:36, ahora devuelve la fila correcta filtrada), `devolver_todas_las_conexiones(id_cable)` (GTK y pantallas_cables.py, sin cambios de resultado), `devolver_todas_las_conexiones()` sin filtro (pantallas_busqueda_global.py, sin cambios), `devolver_todas_las_conexiones(id_cable=...)` por kwarg (pantallas_conexiones.py:189, sin cambios); además casos límite: ambos filtros combinados (AND correcto) e id_equipo inexistente (lista vacía, sin excepción). No se corrió Xvfb con Kivy real esta entrega (fix acotado a una función de core, sin UI nueva ni cambios de layout). APP_VERSION -> 1.20260916080000.
2026-09-17T03:15 ui_gtk/equipos_ui.py: reorganización de `_DialogoEquipo` (ficha de equipo) pedida por Fede a partir de una captura de pantalla — el diálogo tenía demasiados botones y campos apilados y no entraban en pantalla. El grid de la pestaña "Datos" tenía ~24 filas (identidad básica + Dimensiones físicas + Riesgo de falla + "Es módulo de frame") sin ScrolledWindow propio (sólo "Configuraciones" scrolleaba), y debajo del Notebook había 3 filas fijas de Gtk.Box con hasta 12 botones de ancho variable (incluyendo etiquetas largas como "Edición masiva conectores en imagen") que se cortaban horizontalmente en ventanas angostas. Cambios: (1) la pestaña "Datos" se acortó a identidad básica (nombre/marca/tipo/modelo/inventario/serie/manual/foto/fecha) envuelta en su propio Gtk.ScrolledWindow; (2) Dimensiones físicas + Riesgo de falla + "Es módulo de frame" se movieron a una pestaña nueva "Riesgo y ubicación", también con su propio scroll; (3) los ~12 botones de acción se movieron a una pestaña nueva "Acciones", agrupados por tema (Conectores / Conexiones / Otros) dentro de Gtk.FlowBox en vez de Gtk.Box+HBox fijas — FlowBox recalcula solo cuántos botones entran por fila según el ancho disponible, así que nunca se cortan; la etiqueta "Edición masiva conectores en imagen" se acortó a "Editor masivo (imagen)" con el texto completo movido al tooltip; la pestaña "Acciones" sólo se agrega si `id_equipo` (alta nueva no tiene acciones todavía, mismo criterio que ya tenía cada botón individual). `set_default_size` bajó de (700, 620) a (760, 560) porque cada pestaña scrollea por separado y ya no hace falta tanto alto. Sin cambios de lógica de negocio, callbacks, orden de guardado ni del pie del diálogo (auditoría/última edición/Cancelar-Aceptar). Validación: (1) ast.parse OK; (2) pyflakes comparado contra baseline (git stash sobre HEAD de este merge): los mismos 7 avisos preexistentes del archivo (imports `re`/`subprocess`/`MANUALES_DIR` sin usar a nivel de módulo + sus 4 redefiniciones dentro de métodos), cero hallazgos nuevos; (3) smoke test visual con GTK real queda pendiente en la máquina de Fede (no hay typelib de Gtk 3.0 disponible en este sandbox, mismo límite ya documentado en sesiones previas).
2026-09-17T03:15 cabledoc.py: sólo APP_VERSION → 1.20260917031500.
2026-09-17T04:10 pantallas_comunes.py: `_pack_auditoria()` ahora arma una sola línea con [fecha última edición] .......... [estado de auditoría] [botón Auditado], en vez de dos filas apiladas (una para la fecha de edición vía `_pack_ultima_edicion`, otra para auditoría). Pedido de Fede sobre la ficha de equipo, pero al ser un helper compartido el fix quedó a nivel de `pantallas_comunes.py` en vez de duplicarlo — afecta a los 4 diálogos que llaman a ambas funciones juntas: `equipos_ui.py` (equipo), `cables_conexiones_ui.py` (cable y conexión) y `conectores_ui.py` (conector). En los 4 se sacó la llamada separada a `_pack_ultima_edicion` (y el import, donde quedaba sin otro uso en el archivo) porque ahora `_pack_auditoria` ya la incluye. Los diálogos que sólo llaman a `_pack_ultima_edicion` sin auditoría (frames_slots_ui.py, racks_salas_ui.py, catalogos_basicos_ui.py, catalogo_simbolo_conector_ui.py — entidades no auditables) no se tocaron.
2026-09-17T04:10 ui_gtk/equipos_ui.py (tres cambios más, mismo pedido): (1) la pestaña "Riesgo y ubicación" (agregada ayer) pasó al final del Notebook — sigue construyéndose junto con "Datos" como antes, sólo se movió la llamada a `nb.append_page()`, que es la que define el orden visual de las pestañas, no el orden de construcción del contenido; orden final: Datos / Configuraciones / Acciones / Riesgo y ubicación. (2) Las filas "Manual (PDF)" y "Foto (Picon)" tenían sus dos botones (seleccionar + Ver/Quitar) en columnas 2 y 3 separadas del grid, mientras que ninguna otra fila usa la columna 3 — se veía como una columna extra sobresaliendo sólo en esas dos filas. Se agruparon ambos botones de cada fila en un único Gtk.Box dentro de la columna 2, igual que el resto de las filas del formulario (entry en columna 1, un solo elemento de acción en columna 2). Sin cambios de comportamiento en los callbacks (`_sel_manual`/`_ver_manual`/`_sel_picon`/`_quitar_picon`, sin tocar). Validación: ast.parse + pyflakes comparado contra baseline (git stash) en los 4 archivos tocados (`equipos_ui.py`, `pantallas_comunes.py`, `cables_conexiones_ui.py`, `conectores_ui.py`) — cero hallazgos nuevos, `pantallas_comunes.py`/`cables_conexiones_ui.py`/`conectores_ui.py` quedaron sin ningún warning. Smoke visual con GTK real pendiente en la máquina de Fede (sin typelib de Gtk 3.0 en este sandbox).
2026-09-17T04:10 cabledoc.py: sólo APP_VERSION → 1.20260917041000.
2026-09-17T05:00 PROGRESS.md: auditoria del roadmap de cierre mobile (PLAN_CIERRE_ROADMAP_MOBILE.md) contra main 09ff19a - items 1/2/3 y 4a ya mergeados (fix vista previa, Fase 4 ABM 6 dialogos, Fase 5.2 riesgo, PlanosListado); solo faltan 4b (MueblesListado) y 4c (VistaPlanoInteractivo mobile). Se documenta el estado real y se deja pendiente de priorizar con Papi/Fede.
2026-09-17T05:00 ui_kivy/main.py: Fase A de plan_ux_botonera_mobile_v1.md - BarraInferior swap Equipos->Cables y Buscar->Conexiones (CableDocApp.on_start), ambos ya accesibles a 1 toque desde Inicio/barra superior por lo que dejan de necesitar slot fijo; Cables/Conexiones son los verbos de mayor rotacion diaria segun el panel de pendientes de Inicio. Iconos cables.png/conexiones.png ya existian en assets/iconos, sin trabajo de diseno nuevo. Validado ast.parse+py_compile+pyflakes contra baseline (git stash): mismas 7 advertencias preexistentes, cero nuevas.
2026-09-17T(actual) ui_kivy/tema.py: Fases B y D de plan_ux_botonera_mobile_v1.md. Fase B (badge de pendientes, S2.2): _ItemNav ahora admite un badge numerico superpuesto sobre el icono (circulo color "error", texto blanco, oculto si valor=0, "99+" como tope) via el nuevo metodo fijar_badge(); BarraInferior.actualizar_badge(id_item, valor) lo busca por id_item (el nombre de icono pasado al crear cada item, ej. "cables"). Fase D (FAB contextual, S2.3): _ItemFAB ahora respeta su propiedad .disabled heredada de ButtonBehavior con atenuacion visual (opacity 0.4) via BarraInferior.fijar_fab_disabled(valor) - usado para apagar el + mientras hay un listado abierto en modo_seleccion=True. Sin cambios de comportamiento en items existentes sin badge/sin disabled explicito.
2026-09-17T(actual) ui_kivy/main.py: Fases B, C y D de plan_ux_botonera_mobile_v1.md. Fase B: PanelPendientesCables.actualizar() ahora tambien actualiza el badge de "Cables" en la barra inferior (mismo total temporales+sin_conexion que ya mostraban las tarjetas de Inicio) + refresco liviano cada 60s via Clock.schedule_interval en CableDocApp.on_start (cubre quedarse en otra pantalla sin pasar por Inicio, alternativa que el propio plan dejaba abierta a instrumentar cada cierre de Cables/Conexiones). Fase C (S3, "reordenar, no rediseñar"): _abrir_menu_completo() - el grupo "Buscar" suma atajos a Cables/Conexiones (mismas funciones ya importadas, sin duplicar logica); "Diagramas" se separo en "Diagramas - Ver" (Imagen con conectores/Arbol/Patcheras) y "Diagramas - Analisis" (Diagrama de conexiones); Preferencias/Aplicacion siguen al final. Fase D (S2.3, tabla ya cerrada con Papi): _abrir_menu_rapido() paso de fijo a contextual via _popup_activo() (primer Popup real en Window.children, mismo criterio que ya usaba _elevar) + registro _opciones_fab_para(popup) por isinstance: DialogoEquipo -> Nuevo conector; EquiposListado -> Equipo nuevo/Alta rapida; RacksListado -> Nuevo rack; ConexionesListado -> Nueva Conexion; sin match (Inicio incluido) -> mismas 3 opciones de siempre (_opciones_fab_default). modo_seleccion=True (EquiposListado/RacksListado/ConexionesListado abiertos como selector desde otro dialogo) deshabilita el + (BarraInferior.fijar_fab_disabled) enganchado en el mismo _elevar que ya corria en cada cambio de Window.children - no se agrego un listener nuevo. Nuevos imports: DialogoEquipo (pantallas_equipos), DialogoConector (pantallas_conectores, modulo nuevo importado en main.py), DialogoConexion (pantallas_conexiones), DialogoRack (pantallas_racks). Bug encontrado y corregido en el propio desarrollo: _popup_activo() referenciaba Window sin importarlo (pyflakes undefined name) - resuelto con el mismo import local que ya usa _ir_a_inicio en este archivo, sin agregar un import a nivel de modulo que no existia antes. Validado: ast.parse+py_compile+pyflakes contra baseline (mismas 7 advertencias preexistentes en main.py, mismo unico warning preexistente en tema.py, cero nuevas) y smoke test real bajo Xvfb/Kivy: _abrir_menu_completo() arma los grupos nuevos correctamente, badge se actualiza y refleja texto/visibilidad, _opciones_fab_para() devuelve las opciones correctas para None/EquiposListado, y el FAB se deshabilita/rehabilita correctamente ante modo_seleccion=True vía el ciclo real de Window.children (Clock real, no simulado).
2026-09-17T(actual) PROGRESS.md: Fases B/C/D de plan_ux_botonera_mobile_v1.md entregadas (siguen a la Fase A ya mergeada). Fase E ("Mas" contextual para DiagramaConexiones, grupos Analizar/Escenario + checks de estado + iconos nuevos) queda para la proxima sesion - es la mas grande y riesgosa del plan (25 botones, varios modos con exclusion mutua), y el reemplazo de emojis por iconos PNG (S5) sigue sin metafora visual definida con Papi (pregunta abierta S7.5).
2026-09-18T14:00 ui_kivy/main.py: agregado_extra.txt (plan_ux_botonera_mobile_v1.md, navegación). (1) Nuevo `_ir_a_inicio_confirmando()`: el ítem 'Inicio' de la barra inferior pide confirmar() si hay más de un Popup apilado antes de llamar a `_ir_a_inicio` (que sigue sin cambios para otros llamadores, p.ej. cambio de tema). (2) `_abrir_menu_mas()` suma al tope el ítem fijo 'Cerrar ventana actual': cierra sólo el Popup de más arriba (capturado antes de abrir el menú, porque `_popup_activo()` devolvería el propio menú durante su animación de cierre); deshabilitado si no hay pantalla abierta.
2026-09-18T14:00 ui_kivy/widgets_base.py: `barra_superior_dialogo()` gana el parámetro opcional `on_buscar` (ícono 'buscar' entre título y menú) — aditivo, firma compatible.
2026-09-18T14:00 ui_kivy/pantallas_equipos.py: `DialogoEquipo` pasa `on_buscar=self._abrir_busqueda_global` (nuevo método, import diferido de abrir_busqueda_global): la búsqueda global se abre encima del formulario sin perder lo editado.
2026-09-18T14:00 ui_gtk/cabledoc.py: sólo APP_VERSION → 1.20260918140000.
2026-09-18T15:00 ui_kivy/pantallas_diagrama.py: Fase E de plan_ux_botonera_mobile_v1.md (§4). Los 15 botones de 'Analizar' (Diagnóstico, Historial, Señal, Leyenda, Riesgo equipo, Riesgo señal, Simular falla, Vista previa) y 'Escenario' (Modo, Reconectar, Nuevo, Abrir, Guardar, Aplicar, Descartar) salen de la toolbar (queda inline sólo Encuadrar/Expandir/Recargar/Solo nombre/Conexión interna/Zoom/PNG). Los widgets se siguen construyendo (los ToggleButton son el modelo de estado `.state` que leen/escriben los handlers y sus exclusiones mutuas) pero sin agregarse al árbol; se registran en `self._items_mas`. Nuevo `DiagramaConexiones.grupos_menu_mas()` devuelve los grupos con '✓' según `btn.state` al abrir el menú; cada ítem dispara `btn.trigger_action(0)` (mismos handlers que el toque directo). Etiquetas de Escenario desambiguadas en el menú (p.ej. '🆕 Esc.' → 'Nuevo escenario').
2026-09-18T15:00 ui_kivy/main.py: `_abrir_menu_mas()` deja de repetir el menú completo (ya vive en el hamburguesa de la barra superior): ahora sólo 'Cerrar ventana actual' + los grupos que exponga la pantalla activa vía `grupos_menu_mas()` (duck typing, extensible a otras pantallas). Alto del popup proporcional a la cantidad de filas.
2026-09-18T15:00 ui_gtk/cabledoc.py: sólo APP_VERSION → 1.20260918150000.
2026-09-18T16:00 ui_kivy/generar_iconos.py + assets/iconos/riesgo.png: nuevo `icono_riesgo()` (triángulo de alerta con exclamación), metáfora elegida por Fede para 'riesgo' (§5 de plan_ux_botonera_mobile_v1.md). Generado sólo ese PNG (no se regeneró el resto).
2026-09-18T16:00 ui_kivy/pantallas_diagrama.py: `_items_mas`/`grupos_menu_mas()` aceptan ícono + clave de color opcionales; Riesgo equipo (alerta), Riesgo señal (primario) y Simular falla (error) usan `riesgo.png` en el "Más" en lugar de los emoji 🎨/🔺. Diagnóstico/Escenario/Señal siguen con emoji (metáfora sin definir).
2026-09-18T16:00 ui_kivy/main.py: `_abrir_menu_mas()` soporta ítems con ícono (IconoImg a la izquierda, texto corrido por padding). `auto_dismiss=True` explícito en el menú "Más" y en el menú "+" (`_abrir_menu_rapido`): tocar afuera los cierra (Popup.__init__ está parcheado con auto_dismiss=False global para evitar cierres accidentales por gestos de Android; los menús chicos son la excepción).
2026-09-18T16:00 ui_kivy/pantallas_equipos.py: menú ⋮ de `DialogoEquipo` con `auto_dismiss=True` (mismo criterio).
2026-09-18T16:00 ui_gtk/cabledoc.py: sólo APP_VERSION → 1.20260918160000.
2026-09-18T23:38 core/modelo.py (Modelo.devolver_pendientes_equipos): nueva clave "sin_picon" = equipos (id_equipo != 0) con equipo.picon NULL o vacío, EXCLUYENDO los de tipo FANTASMA (rol_senal, no por nombre): son placeholders de un extremo desconectado, no un equipo físico al que se le pueda sacar foto, y el ABM los oculta por defecto — así el número del dashboard coincide con lo que se ve al tocar "ver →". Cambio aditivo: las claves existentes (incluida "sin_imagen", que sigue usando ui_kivy/main.py) no cambian.
2026-09-18T23:38 core/modelo.py (Modelo.devolver_equipos_fantasma): nuevo, gemelo de devolver_equipos_patchera() — equipos cuyo tipo tiene rol_senal='FANTASMA'. Lo usa el filtro "Ocultar fantasmas" del ABM de Equipos (GTK).
2026-09-18T23:38 ui_gtk/cabledoc.py (VentanaPrincipal._actualizar_panel_pendientes_eq): en "Trabajo pendiente — Equipos" se reemplaza el tile "🖼 Sin imagen" (equipo.id_imagen, mecanismo legado dado de baja en la Fase 8 de plan_desarrollo_ubicacion_fisica_planos.md y reemplazado por la ubicación física) por "📷 Sin picon", que abre EquiposListado(filtro_pendiente="sin_picon"). Se mantiene "📍 Sin imagen c/ conect." (conector.id_imagen sigue vigente, no es el mismo mecanismo). El panel de Frames no se tocó. APP_VERSION → 1.20260918233800.
2026-09-18T23:38 ui_gtk/equipos_ui.py (EquiposListado): (1) nuevo filtro_pendiente="sin_picon" (título "Equipos — Sin picon", mismo criterio SQL que el contador del dashboard). La rama "sin_imagen" queda en el código sin cambios (ya no la invoca el dashboard GTK, se deja por compatibilidad). (2) nuevo checkbox "Ocultar fantasmas", tildado por defecto, mismo estilo que "Ocultar patcheras": atributo _ocultar_fantasmas (definido antes de super().__init__, igual que _ocultar_patcheras), _ids_fantasma cargado en cargar_datos() vía Modelo.devolver_equipos_fantasma(), y se suma en _filtrar(). Los dos checkboxes son independientes entre sí. Aplica también a los llamadores en modo_seleccion (igual que patcheras): para elegir un fantasma hay que destildar el checkbox.
2026-09-18T23:38 core/i18n.py: 3 claves nuevas EN/PT — "📷 Sin picon", "Equipos — Sin picon", "Ocultar fantasmas".
2026-09-18T23:38 Validación: ast.parse + py_compile + pyflakes vs baseline (git stash) sobre core/modelo.py, core/i18n.py, ui_gtk/cabledoc.py, ui_gtk/equipos_ui.py — cero hallazgos nuevos (136 antes / 136 después). Smoke test real bajo Xvfb con GTK 3 y fixture SQLite armado desde data/schema_db.sql (cámaras con/sin picon —NULL y sólo espacios—, 2 FANTASMA, 1 PATCHERA): sin_picon=3 (2 cámaras + patchera; fantasmas excluidos); ABM por defecto no lista fantasmas ni patcheras; destildar "Ocultar fantasmas" los muestra; los dos checkboxes son independientes; EquiposListado(filtro_pendiente="sin_picon") lista 2 (3 con patcheras visibles); el panel de pendientes de Equipos muestra "Sin picon" y ya no "Sin imagen". Pendiente: prueba visual en la máquina de Fede (disposición del checkbox nuevo en la barra de botones del ABM).
2026-09-19T00:00 ui_gtk/planos_ui.py: nueva función `abrir_vista_plano(parent, id_plano=None)` — punto de entrada directo a VistaPlanoInteractivo desde la GUI (hasta ahora la ventana del plano sólo se alcanzaba de rebote desde el diálogo de una sala/rack/mueble/equipo). Sin id_plano: avisa si no hay planos cargados, abre directo si hay uno solo, y con varios pregunta cuál vía `_elegir_plano` (combo mínimo, ordenado como Modelo.devolver_todos_los_planos). PlanosListado suma el botón extra "🗺 Ver plano" (abre el plano de la fila seleccionada; oculto en modo_seleccion, donde el listado es sólo un selector).
2026-09-19T00:00 ui_gtk/cabledoc.py: nuevo ítem "🗺 Vista de plano…" en Infraestructura (junto a "🖼 Vista gráfica de rack…") con handler `_abrir_vista_plano`; import de `abrir_vista_plano` sumado al bloque de planos_ui. APP_VERSION → 1.20260919000000.
2026-09-19T00:00 core/i18n.py: 6 claves nuevas EN/PT ("🗺 Vista de plano…", "Ver plano", "Elegir plano", "Plano:", y los dos mensajes de aviso de abrir_vista_plano/PlanosListado).
2026-09-19T00:00 ui_kivy/pantallas_busqueda_global.py: la búsqueda global ahora filtra los equipos por "<nombre> <marca> <tipo> <modelo> <inventario> <serie>" (campos vacíos omitidos), no sólo por nombre. _NodoGlobal gana `busqueda` (texto de matcheo en minúsculas, calculado una vez; por defecto el texto visible, así el resto de los nodos no cambia). _construir_arbol arma un dict id_equipo→texto con UNA consulta en bloque (equipo + marca + tipo_equipo) y lo pasa a los 4 lugares donde se crea un nodo de equipo (en frame, directo en rack, suelto de sala, sin ubicación). El texto visible del nodo sigue siendo el nombre. El filtro pasa a ser por palabras (todas deben aparecer, en cualquier orden) en vez de subcadena contigua, para poder combinar campos ("sony 3500"); para nodos que no son equipo equivale a lo anterior salvo que ahora también matchea con las palabras en otro orden.
2026-09-19T00:00 ui_gtk/panel_arbol_ui.py (PanelArbol, panel lateral "Infraestructura" de la ventana principal): mismo cambio que la búsqueda global mobile (ui_kivy/pantallas_busqueda_global.py) — el filtro ahora matchea cada equipo por "<nombre> <marca> <tipo> <modelo> <inventario> <serie>" (campos vacíos omitidos), no sólo por nombre. _nodo() gana `busqueda` (texto de matcheo en minúsculas, calculado una vez; por defecto el label, así los demás nodos no cambian) y _cargar_arbol_datos arma un dict id_equipo→texto con UNA consulta (equipo + marca + tipo_equipo) que pasa a los 4 lugares donde se crea un nodo de equipo (en slot de frame, directo en rack, suelto de sala, sin ubicación). El label visible sigue siendo el nombre. _renderizar_store_desde_cache filtra por palabras (todas deben aparecer, en cualquier orden) en vez de subcadena contigua. Nota: el docstring del módulo dice que PanelArbol "no tiene consumidor externo" — es cierto para otros módulos, pero VentanaPrincipal lo instancia (cabledoc.py: self._panel_arbol = PanelArbol(self)) y es el panel lateral que se ve al abrir la app.
2026-09-19T00:00 ui_gtk/panel_arbol_ui.py + ui_kivy/pantallas_busqueda_global.py (corrección de las dos entradas anteriores de búsqueda por campos): el texto de cada nodo de equipo ahora ES la cadena "<nombre> <marca> <tipo> <modelo> <inventario> <serie>" (campos vacíos omitidos; p.ej. "CONSOLA DE ESTUDIO YAMAHA CONSOLA M7CL-48-ES 65715 WTARK01006") tanto al armar el árbol como al recargarlo, y el filtro sigue matcheando por palabras sobre ese texto. Antes sólo el texto de matcheo llevaba los campos y el label visible era el nombre. Se elimina el parámetro/kwarg `busqueda` de _nodo (GTK) y _NodoGlobal (Kivy): la clave/atributo `busqueda` pasa a ser simplemente el label en minúsculas. Kivy: _FilaArbolGlobal mide el texto de los nodos de equipo (kivy.core.text.Label.get_extents) y ensancha el label de esa fila hasta _ANCHO_LABEL_MAX=dp(900) para que no se corte con "…" a los 200dp; el resto de los nodos conserva _ANCHO_LABEL.
2026-09-20T00:00 ui_kivy/pantallas_diagrama.py: el minimapa del diagrama de conexiones quedaba tapado por la botonera inferior global. Causa: _reubicar_minimap asignaba `_minimap.pos = (cw - mw - dp(8), dp(8))` — `pos` de un hijo de FloatLayout con size_hint=None es absoluta en la Window, no relativa al contenedor, así que el minimapa caía a dp(8) del borde inferior de la PANTALLA (bajo los 60dp de la barra) en vez de a dp(8) de la esquina inferior derecha del canvas_cont; además sólo escuchaba `size`, no `pos`. Fix: pos = (canvas_cont.x + cw - mw - dp(8), canvas_cont.y + dp(8)) y bind a size y pos. Medido en Xvfb (370x826, barra de 60dp): minimapa y=8 → dentro del canvas_cont, sobre la barra.
2026-09-20T00:00 ui_gtk/export_diagrama_ui.py: la exportación del diagrama de conexiones (SVG/PDF) sale con fondo transparente. `_exportar_renderizar` ("Todo el diagrama", el que usa el menú Exportar) y `_exportar_vista` ("Vista actual") ya no pintan el fondo oscuro (C_BG) ni la grilla (C_GRID); la superficie de Cairo arranca vacía y sólo se dibujan cables, nodos y etiquetas. La pantalla (_on_draw) no cambia. Las etiquetas de cable siguen llevando su cajita semitransparente de C_BG detrás del texto, así que se leen igual sobre fondo claro.
2026-09-20T00:00 ui_gtk/export_diagrama_ui.py + ui_gtk/diagrama_conexiones_ui.py + core/i18n.py: el diagrama de conexiones GTK suma "🖼 Exportar como PNG" (menú Exportar, junto a SVG y PDF) con fondo transparente, para "Todo el diagrama" (_exportar) y "Vista actual" (_exportar_vista). Nuevos helpers `_exportar_crear_superficie` / `_exportar_cerrar_superficie` en ExportMixin (los tres formatos pasan por ahí, se elimina la duplicación SVG/PDF de los dos métodos): PNG usa cairo.ImageSurface ARGB32 (arranca transparente) a escala 2x (PNG_ESCALA) para que no se pixele al ampliar, acotada por PNG_MAX_PIXELES=64M px y PNG_MAX_LADO=30000 para diagramas grandes (la escala baja incluso de 1 si hace falta) y se escribe con write_to_png antes de finish(). Nueva tabla _DESC_FORMATO para los títulos de los diálogos ("PNG transparente"). i18n: 1 clave EN/PT para el ítem de menú.
2026-09-21T00:00 core/modelo.py: nuevo Modelo.color_escala_auditoria(fecha) — escala de color FIJA (no relativa al resto de la base) para representar la antigüedad de una auditoría de campo: celeste claro (recién auditada) a azul muy oscuro (auditada hace más de AUDITORIA_ESCALA_DIAS_MAX=365 días, o NUNCA auditada — mismo tono para ambos casos, a propósito). Nuevo Modelo.devolver_fechas_auditoria_equipos() — {id_equipo: ultima_auditoria_fecha} de todos los equipos reales en UNA consulta. Usados por el toggle "colorear por auditoría" nuevo en diagrama de conexiones y diagrama de patcheras (GTK y Kivy).
2026-09-21T00:00 ui_gtk/auditoria_diagrama_ui.py (nuevo) + ui_gtk/diagrama_conexiones_ui.py + ui_gtk/dibujo_diagrama_ui.py: diagrama de conexiones GTK suma el toggle "🕓 Colorear por auditoría" (menú Ver, mismo patrón que SenalDiagramaMixin/senal_diagrama_ui.py): pinta la cabecera de cada nodo con Modelo.color_escala_auditoria según ultima_auditoria_fecha del equipo. Aplicado en _draw_node ANTES de _riesgo_color_y_borde, para que un nodo en riesgo se siga resaltando aunque el toggle esté prendido. Sin leyenda en el canvas (ya ocupado por señal/riesgo/impacto/escenario/minimapa/vista previa); la escala se explica en el tooltip del ítem de menú.
2026-09-21T00:00 ui_kivy/pantallas_diagrama.py: mismo toggle "🕓 Auditoría" en el diagrama de conexiones mobile, agregado al menú "Más → Analizar" (no a la fila visible, ya cargada de botones). Nuevo helper _hex_a_rgb01. _toggle_auditoria carga Modelo.devolver_fechas_auditoria_equipos()+color_escala_auditoria a self._auditoria_cache; _draw_node de _CanvasDiagrama aplica el color ANTES del color de riesgo (mismo criterio que GTK).
2026-09-21T00:00 ui_gtk/patcheras_ui.py: PatcherasVista (diagrama de patcheras) suma el toggle "🕓 Auditoría" en la barra superior, disponible en los dos modos (por equipo y global): en vez de verde/rojo/oscuro por estado de conexión, cada punto se pinta según Modelo.color_escala_auditoria del EQUIPO instalado en ese slot (mismo id_equipo/id_equipo_modulo que ya identifica el módulo patchera de la columna en ambos modos). El tooltip del modo por-equipo muestra la fecha real de auditoría en vez del estado de conexión cuando el toggle está activo; el click (_on_click) y el tooltip del modo global no se tocaron, siguen funcionando en base al color de conexión guardado en cols (el color por auditoría es sólo visual, no reemplaza esos datos).
2026-09-21T00:00 ui_kivy/pantallas_vistas.py: mismo toggle "🕓 Auditoría" en PatcherasVista (Kivy), junto al botón PNG. _cargar_datos_patcheras ahora incluye id_equipo por columna (antes sólo tenía el color "green"/"red"/"dark"). _PatcheraStripWidget gana auditoria_activo/auditoria_cache + set_auditoria() (PatcherasVista._toggle_auditoria lo propaga a todos los strips vivos; _cargar() lo hereda en los strips que reconstruye). El status del click también muestra la fecha de auditoría en vez del estado de conexión cuando el toggle está activo. Nuevo helper _hex_a_rgba01 (4-tupla, formato de C_GREEN/C_RED/C_DARK de este módulo).
2026-09-22T00:00 core/modelo.py: nuevo Modelo.devolver_editados_sin_auditar(tabla="equipo") (Grupo B, B1 de plan_auditoria_fecha_edicion_v1.md) — registros de una tabla auditable cuya fecha_ultima_edicion es más nueva que ultima_auditoria_fecha, o que nunca fueron auditados (con fecha_ultima_edicion IS NOT NULL como guarda: no marca registros sin ninguna edición). Devuelve {str(pk): {"fecha_edicion", "fecha_auditoria"}}. Base para B2 (GTK) y B3 (Kivy), sin UI todavía.
2026-09-22T00:00 ui_gtk/equipos_ui.py: EquiposListado suma columna "Auditoría" (Grupo B, B2 de plan_auditoria_fecha_edicion_v1.md) — badge "✏️ Editado sin auditar" para equipos que Modelo.devolver_editados_sin_auditar("equipo") (B1) marca como editados después de su última auditoría de campo (o nunca auditados con alguna edición registrada); celda vacía en el resto. Nuevo helper estático _agregar_columna_auditoria, llamado en cargar_datos justo después de _agregar_columna_riesgo.
2026-09-22T00:00 ui_kivy/pantallas_equipos.py: EquiposListado (mobile) suma el mismo badge que B2 (Grupo B, B3 de plan_auditoria_fecha_edicion_v1.md), reusando el chip existente en la tarjeta en vez de un elemento nuevo (no hay lugar en el layout para una columna). cargar_datos calcula self._editados_sin_auditar con Modelo.devolver_editados_sin_auditar("equipo") (B1); _refiltrar prioriza ese chip ("Editado sin auditar", alerta) sobre "Auditado"/"No auditado" cuando corresponde.
2026-09-23T00:00 core/modelo.py: nuevo Modelo.devolver_cobertura_auditoria(dias=90) (Grupo D, D1 de plan_auditoria_fecha_edicion_v1.md) — cobertura de auditoría agrupada por rack (no por sala: se resuelve en una sola consulta agregada vía posicion_en_rack/slot, evitando la resolución de ubicación física por-equipo que no sirve para agregar en bloque). Contempla equipos directos en el rack y módulos en slots de frames rackeados. Devuelve {id_rack: {"nombre","total","auditados","porcentaje"}}, con "auditados" = equipos cuya ultima_auditoria_fecha cae dentro de los últimos `dias` días. Base para D2 (GTK) y D3 (Kivy), sin UI todavía.
2026-09-23T20:45 ui_gtk/cobertura_auditoria_ui.py (nuevo): CoberturaAuditoriaListado(VentanaListado) + abrir_cobertura_auditoria() (Grupo D, D2 de plan_auditoria_fecha_edicion_v1.md) — reporte de sólo lectura de Modelo.devolver_cobertura_auditoria() (D1): una fila por rack con Auditados / Total / Cobertura (%), ordenado de menor a mayor cobertura. Sin Agregar/Editar/Eliminar (set_no_show_all para que run()/show_all() no los rehabilite); combo "Ventana:" (últimos 30/60/90/180/365 días, default 90) que recalcula al cambiar, y una línea de resumen global ("X de Y equipos auditados en los últimos N días"; el tooltip aclara que sólo cuenta equipos en racks, no todos los de la base). Las columnas de conteo/porcentaje ordenan por valor numérico (el orden natural de VentanaListado sólo entiende enteros y dejaría "9.0 %" después de "87.5 %").
2026-09-23T20:45 ui_gtk/cabledoc.py: nuevo ítem "🕓 Cobertura de auditoría…" al final del menú Infraestructura (handler VentanaPrincipal._abrir_cobertura_auditoria, import diferido de cobertura_auditoria_ui, mismo patrón que _abrir_zonas_sospechosas). APP_VERSION → 1.20260923204500.
2026-09-23T20:45 core/i18n.py: 10 claves nuevas EN/PT para el reporte de cobertura de auditoría (ítem de menú, título, columnas, combo de ventana, resumen y aviso de sin-datos, tooltip).
2026-09-23T20:45 Validación D2: ast.parse + py_compile + pyflakes vs baseline (git stash) sobre core/i18n.py, ui_gtk/cabledoc.py y el archivo nuevo — cero hallazgos nuevos (129 antes / 129 después). Smoke test real bajo Xvfb con GTK 3 y fixture SQLite armado desde data/schema_db.sql (3 racks + 1 vacío, un frame rackeado con 2 módulos en slots, un equipo suelto): cobertura a 90 días C=0.0 %, B=25.0 %, A=75.0 % en ese orden y resumen global 4 de 9 (44.4 %); el rack vacío y el equipo suelto no aparecen; a 180/365/30 días los porcentajes de B coinciden con el cálculo a mano (50.0 / 75.0 / 25.0 %); botones CRUD siguen ocultos después de show_all(); orden numérico y filtro de texto OK; sin racks muestra el aviso; el ítem aparece en el menú Infraestructura de VentanaPrincipal y su handler abre el reporte. Pendiente: prueba visual en la máquina de Fede (disposición de la fila Ventana/resumen sobre la barra de filtro).
2026-09-23T21:15 ui_kivy/pantallas_cobertura_auditoria.py (nuevo): CoberturaAuditoriaListado(ListadoPopup) + abrir_cobertura_auditoria() (Grupo D, D3 de plan_auditoria_fecha_edicion_v1.md) — equivalente mobile del reporte D2: cobertura de auditoría por rack (Modelo.devolver_cobertura_auditoria, D1), de sólo lectura y ordenado de menor a mayor cobertura. Adaptaciones: Auditados/Total juntos en una columna ("3 / 4") para que la tabla entre en 360dp; ventana de días (30/60/90/180/365, default 90) como ToggleButton en scroll horizontal (mismo patrón que el filtro de estado de CablesListado); se quita la fila de botones Agregar/Editar/Eliminar entera en vez de dejar una barra vacía; línea de resumen global bajo el selector. Sin claves i18n nuevas: reusa las de D2.
2026-09-23T21:15 ui_kivy/main.py: ítem "Cobertura de auditoría" al final del grupo Infraestructura de _abrir_menu_completo() (import de abrir_cobertura_auditoria desde pantallas_cobertura_auditoria). Sin cambio de APP_VERSION (sólo ui_kivy, mismo criterio que las entregas mobile anteriores).
2026-09-23T21:15 Validación D3: ast.parse + py_compile + pyflakes vs baseline (git stash -u) sobre ui_kivy/main.py y el archivo nuevo — mismas 7 advertencias preexistentes, cero nuevas. Smoke test real bajo Xvfb con Kivy 2.3.1 y el mismo fixture de D2: a 90 días C="0 / 1" 0.0 %, B="1 / 4" 25.0 %, A="3 / 4" 75.0 % en ese orden, resumen global 4 de 9 (44.4 %); tocar los ToggleButton reales a 180/30 días recalcula (B 50.0 % / 25.0 %); filtro de texto OK; nuevo/editar/eliminar/_on_editar no rompen; sin racks muestra el aviso; el ítem aparece en el grupo Infraestructura de _abrir_menu_completo() y su callback abre la pantalla. Pendiente: prueba visual en dispositivo real (Fede/Pepepompin) — la captura bajo Xvfb sale negra (GL por software), no sirve para revisar el layout.
2026-09-23T21:22 core/modelo.py: nueva tabla config_auditoria (Grupo E, E1 de plan_auditoria_fecha_edicion_v1.md) — mismo DDL y patrón que config_riesgo_analogico (clave TEXT PRIMARY KEY, valor REAL NOT NULL), con el default sembrado dias_sla_auditoria=90.0 (constante Modelo.CONFIG_AUDITORIA_DEFAULTS; INSERT OR IGNORE, no pisa valores ya establecidos). Nuevos Modelo.asegurar_tabla_config_auditoria() (idempotente; la llaman los propios getters/setters, así que no hace falta tocar el arranque de ninguna de las dos UIs), Modelo.devolver_config_auditoria() -> dict {clave: valor} y Modelo.establecer_config_auditoria(clave, valor) (upsert), calcados de devolver_/establecer_config_riesgo_analogico. Un solo valor global; un SLA por tipo de equipo queda para una vuelta aparte. Base para E2 (vencidos según SLA), sin UI todavía.
2026-09-23T21:22 Validación E1: py_compile + pyflakes vs baseline (git show HEAD:core/modelo.py) — 0 antes / 0 después. Smoke test sobre base SQLite armada desde data/schema_db.sql (sin config_auditoria previa): devolver_config_auditoria() crea la tabla y trae {"dias_sla_auditoria": 90.0}; establecer_config_auditoria("dias_sla_auditoria", 60.0) y la relectura (otra conexión) trae 60.0; volver a llamar asegurar_tabla_config_auditoria() dos veces no pisa el 60.0 ni duplica filas; el upsert de una clave nueva no rompe la existente; el DDL guardado en sqlite_master coincide con el de config_riesgo_analogico.
2026-09-23T21:24 core/modelo.py: nuevo Modelo.devolver_vencidos_sla_auditoria() (Grupo E, E2 de plan_auditoria_fecha_edicion_v1.md) — equipos cuya auditoría está vencida según la política: ultima_auditoria_fecha más vieja que dias_sla_auditoria (leído de config_auditoria, E1) o nunca auditados. Vence cuando pasaron MÁS de dias_sla_auditoria días completos (mismo criterio de días enteros que color_escala_auditoria, así el "dias" devuelto siempre supera al SLA); se compara contra 'localtime' porque marcar_auditado guarda hora local. Fecha vacía o ilegible = "nunca auditado" (mismo criterio que color_escala_auditoria; devolver_pendientes_auditoria sólo cuenta NULL/vacía). Excluye id_equipo=0. Si el SLA configurado es inválido (no numérico o negativo) usa el default de CONFIG_AUDITORIA_DEFAULTS. Devuelve {str(id_equipo): {"fecha_auditoria": str|None, "dias": int|None}} (dias None = nunca auditado), mismo estilo de dict que devolver_editados_sin_auditar. Base para E3 (GTK), E4 (Kivy) y E5 (color), sin UI todavía.
2026-09-23T21:24 Validación E2: py_compile + pyflakes vs baseline — 0 antes / 0 después. Smoke test sobre base SQLite armada desde data/schema_db.sql con 11 equipos (placeholder id 0, NULL, '', fecha corrupta, hoy, hace 89d, 90d exactos, 90d+20h, 91d, 400d, fecha futura): con el SLA por defecto (90) vencen 1/2/3 (nunca/corrupta) y 91d/400d — 90d exactos y 90d+20h NO vencen (son 90 días completos) y el placeholder, "hoy" y la fecha futura tampoco; establecer_config_auditoria 60 → suma 89d/90d/90d+20h; 400 → sólo los nunca auditados (400d exactos no es > 400); 0 → todo lo de 1 día o más; SLA -5 y SLA 'abc' guardado como texto → cae al default 90; tabla config_auditoria vaciada → se vuelve a sembrar y usa 90; sin equipos reales → {}. El "dias" devuelto es siempre > SLA.
2026-09-23T21:29 ui_gtk/cabledoc.py: el panel "Trabajo pendiente — Auditoría" de Inicio (A1) suma una segunda fila con la tarjeta "⏰ Vencidos (SLA N d)" (Grupo E, E3 de plan_auditoria_fecha_edicion_v1.md): cantidad de equipos con la auditoría vencida según Modelo.devolver_vencidos_sla_auditoria() (E2) — a diferencia de las 7 tarjetas de arriba ("nunca auditados"), incluye también los auditados hace más de N días. Nuevo método VentanaPrincipal._agregar_tarjeta_sla_auditoria(g, n_cols) (llamado al final de _actualizar_panel_pendientes_aud; si falla no se agrega la tarjeta y las 7 de arriba siguen); botón "ver →" abre EquiposListado(filtro_pendiente="vencidos_sla") — sólo si hay vencidos, porque un listado filtrado sin coincidencias muestra TODOS los equipos (comportamiento previo de EquiposListado._filtrar, no tocado); botón "⚙ SLA…" → nuevo VentanaPrincipal._abrir_config_sla_auditoria, que refresca el panel si se guardó un cambio. APP_VERSION → 1.20260923212728.
2026-09-23T21:29 ui_gtk/equipos_ui.py: EquiposListado acepta filtro_pendiente="vencidos_sla" (título "Equipos — Auditoría vencida"; resalta/filtra los ids de Modelo.devolver_vencidos_sla_auditoria(), mismo criterio que el contador de Inicio).
2026-09-23T21:29 ui_gtk/config_sla_auditoria_ui.py (nuevo): _DialogoConfigSLAAuditoria + abrir_config_sla_auditoria() — diálogo chico con un SpinButton de días (1-3650, precargado con config_auditoria.dias_sla_auditoria) que guarda con Modelo.establecer_config_auditoria y devuelve True si se guardó. Desviación del plan: E3 pedía "un campo numérico en algún diálogo de preferencias existente", pero la app no tiene ningún diálogo de preferencias accesible desde la UI (_DialogoConfigRiesgoAnalogico de bitacora_ui.py existe pero no está enganchado a ningún menú), así que el campo vive en este diálogo propio, abierto desde la tarjeta.
2026-09-23T21:29 core/i18n.py: 7 claves nuevas EN/PT (tarjeta, botón ⚙, tooltip, título del listado filtrado y textos del diálogo; "Cancelar"/"Aceptar" ya existían).
2026-09-23T21:29 Validación E3: py_compile + pyflakes vs baseline (git show HEAD:archivo) — cabledoc.py 93/93, equipos_ui.py 7/7, i18n.py 36/36, config_sla_auditoria_ui.py 0 — cero hallazgos nuevos. Smoke test real bajo Xvfb con GTK 3.24 y base SQLite armada desde data/schema_db.sql (5 equipos: auditados hace 1/10/70/100 días y uno nunca): VentanaPrincipal completa instanciada; el panel conserva sus 7 tarjetas de la fila 0 (Equipos "nunca auditados" = 1, sin cambios) y la fila 1 muestra "2 · ⏰ Vencidos (SLA 90 d)" con "ver →" y "⚙ SLA…"; EquiposListado(filtro_pendiente="vencidos_sla") resalta y muestra exactamente los ids 4 y 5 (regresión: "sin_auditar" sigue mostrando sólo el nunca auditado); el diálogo precarga 90, "Aceptar" con 60 guarda 60.0 y devuelve True, "Cancelar" no cambia nada y devuelve False, el spin acota 0→1 y 99999→3650; el botón ⚙ abre el diálogo con el padre correcto y el panel se refresca solo (pasa a "3 · SLA 60 d", ahora incluye el de 70 días); con 0 vencidos desaparece "ver →" y queda "⚙ SLA…"; 3 refrescos seguidos no acumulan tarjetas (8 en total); con idioma EN el título dice "Overdue (SLA 60 d)". Render offscreen del panel revisado a ojo: la tarjeta SLA queda centrada bajo las 7 de arriba.
